# E11-S2-T1 Handoff: Loyalty Configuration Schema

**Task ID:** E11-S2-T1
**Status:** Completed
**Date:** 2026-03-22

## Changed Surfaces

### Types Package (`packages/types/src/loyalty.ts`)
- `PointAccumulationMode`: `per_dollar` | `per_order`
- `PointExpirationPolicy`: `time_based` | `rolling` | `never`
- `LoyaltyTierDefinition`: `name`, `pointThreshold`, `benefitsDescription`
- `LoyaltyProgramConfig`: tenant-scoped config with tiers, accumulation rules, redemption rate, expiration policy
- `CreateLoyaltyProgramInput` / `UpdateLoyaltyProgramInput`: CRUD input types
- `DEFAULT_LOYALTY_PROGRAM_CONFIG`: sensible defaults (disabled, 4 tiers, per_dollar, rolling 365-day expiration)
- Validation: `validateLoyaltyProgramConfig()` — checks tier ordering, threshold starts at 0, non-negative values
- Type guards: `isValidAccumulationMode()`, `isValidExpirationPolicy()`

### Prisma Schema (`apps/api/prisma/schema.prisma`)
- `PointAccumulationModeEnum`: PER_DOLLAR, PER_ORDER
- `PointExpirationPolicyEnum`: TIME_BASED, ROLLING, NEVER
- `LoyaltyProgramConfig` model with tenant-unique constraint

### Auth Module (`packages/types/src/auth.ts`)
- Added `loyalty` to `tenantModuleKeys` (7 total)
- Added loyalty module registry entry: category=commerce, requires=ordering

## Contracts Introduced
- `LoyaltyProgramConfig` shape — consumed by T3 accumulation, T4 redemption, T5 expiration
- `DEFAULT_LOYALTY_PROGRAM_CONFIG` — used as template for new tenant programs
- `validateLoyaltyProgramConfig()` — used by API layer before save

## Validation
- `pnpm --filter @platform/api typecheck` ✅
- `pnpm typecheck:contracts` ✅
- `pnpm --filter @platform/api test` ✅ (2289 tests)
- `pnpm --filter @platform/types test` ✅ (586 tests)

## Files Changed
- `platform/packages/types/src/loyalty.ts` (new)
- `platform/packages/types/src/loyalty.test.ts` (new)
- `platform/packages/types/src/auth.ts` (modified — module key added)
- `platform/packages/types/src/auth.test.ts` (modified — updated count)
- `platform/apps/api/prisma/schema.prisma` (modified — loyalty models added)
- `platform/apps/api/src/auth/module-capability.service.ts` (modified — loyalty capability)
- `platform/apps/api/src/auth/module-registry.service.test.ts` (modified — updated count)
