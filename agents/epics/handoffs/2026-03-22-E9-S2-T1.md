# E9-S2-T1 Handoff: Import Artifact Domain Model & Format Registry

**Task ID:** E9-S2-T1
**Status:** Completed
**Date:** 2026-03-22

## Changed Surfaces

### Types Package (`packages/types/src/import.ts`)
- `ImportArtifactType` enum: `menu-pdf`, `menu-image`, `service-list-csv`, `catalog-spreadsheet`, `brochure-image`, `price-sheet-pdf`
- `AllowedMimeType` enum: `image/jpeg`, `image/png`, `application/pdf`, `text/csv`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- `artifactTypeToMimeTypes` registry: maps artifact types → accepted MIME types
- `artifactTypeSizeLimits` registry: per-type file size limits (5–20 MB)
- `ArtifactUploadStatus`: `pending`, `scanning`, `stored`, `scan-failed`, `rejected`
- `ImportArtifactRecord`: full artifact schema with tenant FK, storage path, scan result
- Validation functions: `validateArtifactMimeType()`, `validateArtifactFileSize()`, `sanitizeFilename()`
- Storage path generator: `generateArtifactStoragePath()` → `{tenantId}/imports/{artifactId}/{sanitizedFilename}`

### Prisma Schema (`apps/api/prisma/schema.prisma`)
- `ImportArtifact` model with tenant-scoped indexes
- `ImportArtifactType` enum (Prisma)
- `ArtifactUploadStatus` enum (Prisma)

### Types Index (`packages/types/src/index.ts`)
- Added `export * from "./import"`

## Contracts Introduced
- `ImportArtifactRecord` shape (consumed by E9-S2-T2 upload API, E9-S3 pipeline)
- Format registry mapping (consumed by validation logic)
- `sanitizeFilename()` for path traversal prevention
- `GLOBAL_MAX_FILE_SIZE_BYTES` = 20 MB

## Test Evidence
- 36 type tests: format validation, MIME types, size limits, filename sanitization
- `pnpm --filter @platform/types test` — 413 tests pass
- `pnpm typecheck:contracts` — pass
- `pnpm lint` — 0 errors

## Validation Commands
```bash
pnpm --filter @platform/api typecheck
pnpm typecheck:contracts
pnpm --filter @platform/types test
```

## Open Assumptions
- Artifact type list may expand (E9-S3 may add `staff-list-csv`, `policy-text`)
- Storage path format locked: `{tenantId}/imports/{artifactId}/{sanitizedFilename}`

## Follow-up Hooks
- E9-S2-T2: Upload intake API consumes this model
- E9-S3-T1: OCR/extraction reads artifact records and storage paths
