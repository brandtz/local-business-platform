# Handoff Note: E6-S1-T4

## Header

- Task ID: E6-S1-T4
- Task Title: Storefront Read Models — expose storefront read models for active catalog presentation
- Status: completed
- Date: 2026-03-20
- Primary owner lane: API Surface Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-06/E6-S1-T4.md

## What Changed

- Changed surfaces:
  - `platform/apps/api/src/catalog/storefront-catalog.service.ts` — StorefrontCatalogService with getActiveCatalog(tenantId) and getActiveItem(tenantId, slug) methods; returns only active categories with active items, mapped to storefront-safe types (no tenantId, createdAt, updatedAt, or status fields)
  - `platform/apps/api/src/app.module.ts` — registered StorefrontCatalogService as provider and export
- New or updated contracts:
  - StorefrontCatalogResponse — top-level catalog with categories sorted by displayOrder
  - StorefrontCategory — id, name, slug, description?, displayOrder, imageUrl?, items[]
  - StorefrontItem — id, name, slug, description?, displayOrder, variants[], modifiers[], media[]
  - StorefrontItemVariant — id, name, priceCents, isDefault, displayOrder
  - StorefrontItemModifier — id, name, priceCents, isRequired, displayOrder
  - StorefrontMediaReference — id, url, altText?, displayOrder
- New config, env vars, events, or migrations: none

## Validation

- Test layers run: unit (read model shape), integration (tenant isolation, status filtering)
- Key passing cases:
  - Storefront returns only active categories and active items
  - Draft and archived items are excluded
  - Tenant isolation enforced — each tenant sees only their own catalog
  - Storefront types omit internal fields (tenantId, createdAt, updatedAt, status, itemId)
  - Individual item lookup by slug returns only active items
  - Categories sorted by displayOrder
  - Empty catalog gracefully returns empty categories array
  - `pnpm --filter @platform/api test` — 345 tests pass
  - `pnpm --filter @platform/api typecheck` — clean
  - `pnpm typecheck:contracts` — clean
- Known untested cases and reason: HTTP endpoint integration (storefront routes not yet wired); Playwright coverage deferred until web-customer consumes storefront API
- Playwright impact: none currently — storefront API not yet exposed via HTTP routes

## Assumptions and Risks

- Assumptions made: StorefrontCatalogService reads from the same CatalogRepository used by admin services; active filter applied at query level
- Risks introduced or still open: none
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E6-S5 (vertical templates), E6-S6 (domain contract stabilization), E7-S1 (cart and pricing engine)
- Recommended first files or docs to read: platform/apps/api/src/catalog/storefront-catalog.service.ts, platform/packages/types/src/catalog.ts
- Specific blocker or caution for next agent: Storefront types are designed for stability; cart (E7-S1) should consume StorefrontItem.variants for pricing
- Board update required: move E6-S1-T4 to Completed
