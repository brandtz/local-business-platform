# E7-S4-T4: Calendar-View Query Model

## Status: Completed

## Changed Surfaces
- `platform/packages/types/src/booking.ts` — CalendarBookingBlock, CalendarViewQuery, CalendarDayData, CalendarViewResponse types
- `platform/apps/api/src/booking/booking.service.ts` — getCalendarView() method
- `platform/apps/api/src/booking/booking.repository.ts` — getCalendarBlocks() method
- `platform/apps/api/src/booking/booking-api-contracts.ts` — assertValidCalendarViewQuery(), assertValidCalendarViewResponse()

## Calendar View Data Contract

### CalendarViewQuery
```typescript
{
  tenantId: string;
  startDate: string;  // YYYY-MM-DD
  endDate: string;    // YYYY-MM-DD
  staffId?: string;   // optional filter
  serviceId?: string; // optional filter
}
```

### CalendarViewResponse
```typescript
{
  days: CalendarDayData[];  // one entry per date in range, sorted chronologically
  query: CalendarViewQuery; // echo of the query for client reference
}
```

### CalendarDayData
```typescript
{
  date: string;  // YYYY-MM-DD
  blocks: CalendarBookingBlock[];  // sorted by startTime within each day
}
```

### CalendarBookingBlock
```typescript
{
  bookingId: string;
  status: BookingStatus;
  customerName: string | null;
  serviceName: string;
  staffId: string;
  staffName: string;
  startTime: string;  // ISO 8601
  endTime: string;    // ISO 8601
  durationMinutes: number;
}
```

## Query Behavior
- All dates in the range are included (even if empty — blocks: [])
- Blocks within each day are sorted by startTime ascending
- Days are sorted chronologically
- Optional staffId filter restricts to one staff member
- Optional serviceId filter restricts to one service type

## Test Evidence
- Calendar view tests in BookingService: date grouping, staff filter, service filter
- Calendar view contract test: assertValidCalendarViewResponse

## Follow-up Hooks
- E7-S4-T5: Admin calendar view renders CalendarDayViewModel from this data
