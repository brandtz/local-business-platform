# Handoff Note

## Header

- Task ID: E1-S4-T2
- Task Title: define package dependency rules to prevent circular imports and layer inversion
- Status: completed
- Date: 2026-03-15
- Primary owner lane: Foundation Lane
- Supporting lane: none
- Packet path: ../packets/epic-01/E1-S4-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/packages/config/src/index.ts
  - ../../../../platform/packages/config/eslint.config.mjs
  - ../../../../platform/packages/config/src/dependency-rules.test.ts
  - ../../../../platform/packages/config/src/eslint-boundary-config.test.ts
- New or updated contracts:
  - workspace dependency direction is now declared explicitly for apps and shared packages
  - config lint policy now blocks imports into another workspace's private `src` tree
  - manifest-level validation now checks declared internal workspace dependencies against the allowed graph and rejects cycles
- New config, env vars, events, or migrations:
  - no new env vars or migrations

## Validation

- Test layers run:
  - `pnpm --filter @platform/config test`
  - `pnpm --filter @platform/config typecheck`
  - `pnpm --filter @platform/config lint`
- Key passing cases:
  - boundary config test confirms the no-restricted-imports rule is present with workspace-private path patterns
  - dependency graph test confirms each workspace has a declared rule entry
  - dependency graph test confirms declared internal manifest dependencies remain acyclic and within the allowed layer graph
- Known untested cases and reason:
  - no violating sample import was introduced because this task was designed to codify prevention rules without adding intentional breakage fixtures

## Assumptions and Risks

- Assumptions made:
  - manifest-level internal dependency checks plus lint restrictions are sufficient at this stage to establish the boundary system before deeper package reference wiring
- Risks introduced or still open:
  - import direction between public package names is not yet enforced per-source-file by layer-specific rules beyond the private-`src` restriction
  - later tasks may need stronger tooling if cross-package implementation depth grows substantially
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E1-S4-T3
  - E1-S4-T4
- Recommended first files or docs to read:
  - ../../../../platform/packages/config/src/index.ts
  - ../../../../platform/packages/config/eslint.config.mjs
  - this handoff note
- Specific blocker or caution for next agent:
  - keep README guidance aligned with the declared dependency graph instead of introducing examples that imply disallowed package directions
- Board update required:
  - move E1-S4-T2 to Completed and activate E1-S4-T3 when its packet is created