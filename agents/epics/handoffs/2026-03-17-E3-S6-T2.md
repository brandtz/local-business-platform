# Handoff Note

## Header

- Task ID: E3-S6-T2
- Task Title: build platform-admin dashboard shells for tenant list and tenant detail views
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Frontend Systems Lane
- Supporting lane: Platform Control Lane
- Packet path: ../packets/epic-03/E3-S6-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/web-platform-admin/src/tenant-dashboard.ts (new — composable for view state resolution, shell state mapping, and error classification)
  - ../../../../platform/apps/web-platform-admin/src/tenant-dashboard.test.ts (new — 25 tests covering access resolution, list/detail state creation, error mapping, and shell state descriptor mapping)
  - ../../../../platform/apps/web-platform-admin/src/tenant-list-page.ts (new — Vue page component for tenant list with loading, empty, error, and populated states)
  - ../../../../platform/apps/web-platform-admin/src/tenant-detail-page.ts (new — Vue page component for tenant detail with loading, not-found, error, and populated states with health reason and publish-blocked indicators)
  - ../../../../platform/apps/web-platform-admin/src/routes.ts (updated — added /tenants and /tenants/:tenantId routes with privilege-gated access via resolveAdminRouteAccess)
  - ../../../../platform/apps/web-platform-admin/src/routes.test.ts (updated — added 5 route guard tests for tenant dashboard routes)
  - ../../../../platform/apps/web-platform-admin/src/main.ts (updated — added Tenants nav link to app shell)
- New or updated contracts:
  - `TenantListViewState` discriminated union: loading, ready, empty, error, access-denied, auth-required
  - `TenantDetailViewState` discriminated union: loading, ready, not-found, error, access-denied, auth-required
  - View state types consume `PlatformTenantOperationalSummary` from @platform/types for tenant data shape
  - Route guards use `resolveAdminRouteAccess` from @platform/types to enforce platform-admin-only access
  - Error states use `classifyApiError` and `shouldTransitionAuthState` from @platform/sdk for consistent error classification
  - Shell state descriptors use `resolveShellStateDescriptor` from @platform/ui for consistent messaging
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter web-platform-admin test` — 4 files, 39 tests, all passing
  - `pnpm --filter web-platform-admin typecheck` — clean
  - `pnpm typecheck:contracts` — clean
- Key passing cases:
  - anonymous viewers get auth-required redirects on tenant list and detail routes
  - non-platform viewers get access-denied redirects
  - platform-admin viewers get component-backed routes (no redirect)
  - empty tenant list produces empty view state
  - populated tenant list produces ready view state with tenants
  - 401 error produces auth-required state; 403 produces access-denied; 500/null produce error state with retryable flag
  - tenant detail not-found produces custom "Tenant Not Found" shell state
  - shell state descriptors map view states to correct titles and messages
- Known untested cases and reason:
  - no Playwright tests yet; tenant list and detail pages will need browser smoke tests when wired to real data
  - page components are not tested with full DOM rendering (would require jsdom/happy-dom); current tests verify view state logic and route configuration

## Assumptions and Risks

- Assumptions made:
  - initial route components render loading state by default; real data fetching will be wired in a follow-on integration task
  - the `PlatformTenantOperationalSummary` shape from E3-S6-T1 is the correct data contract for tenant list and detail views
  - route guards follow the same pattern as the existing home route (redirect vs component based on access)
- Risks introduced or still open:
  - page components use `h()` render functions without CSS; visual styling will need design system integration
  - tenant detail route uses `:tenantId` param but data fetching is not connected to the route param yet
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S6-T3 (audit, job, publish summary widgets — embeds into these dashboard shells)
  - E3-S6-T4 (filtering and search semantics for tenant ops views)
- Recommended first files or docs to read:
  - ../../../../platform/apps/web-platform-admin/src/tenant-dashboard.ts (view state types and resolution logic)
  - ../../../../platform/apps/web-platform-admin/src/tenant-list-page.ts (list page component structure)
  - ../../../../platform/apps/web-platform-admin/src/tenant-detail-page.ts (detail page component structure)
  - this handoff note
- Specific blocker or caution for next agent:
  - widgets from E3-S6-T3 should embed into the detail page component without breaking the existing view state discriminated union
  - filtering from E3-S6-T4 should extend the list page without altering the route guard pattern
  - do not weaken the platform-admin route guard when adding widget or filter functionality
- Board update required:
  - move E3-S6-T2 to Completed
