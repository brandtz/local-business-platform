# E11-S2-T6 Handoff: Loyalty API Contracts

**Task ID:** E11-S2-T6
**Status:** Completed
**Date:** 2026-03-22

## Changed Surfaces

### API Contracts (`apps/api/src/loyalty/loyalty-api-contracts.ts`)

**Customer-Facing:**
- `assertPointRedemptionInput()`: validates tenantId, customerId, pointsToRedeem (positive number)
- `assertPointLedgerQuery()`: validates tenantId, customerId, optional page/pageSize (1–100)

**Admin-Facing:**
- `assertCreateLoyaltyProgramInput()`: validates tenantId, optional accumulationMode (per_dollar|per_order), expirationPolicy (time_based|rolling|never), numeric fields non-negative, tiers as array
- `assertUpdateLoyaltyProgramInput()`: validates optional update fields with same rules
- `assertManualPointAdjustmentInput()`: validates tenantId, customerId, non-zero points, required reason, required actorId

### Service API (`apps/api/src/loyalty/loyalty.service.ts`)

**Customer-Facing Operations:**
- `getLoyaltyTabData(tenantId, customerId)` → `LoyaltyTabData`
- `redeemPoints(input)` → `PointRedemptionResult`
- `getLedger(tenantId, customerId, type?, page?, pageSize?)` → `PointLedgerResponse`

**Admin-Facing Operations:**
- `getConfig(tenantId)` → `LoyaltyProgramConfig`
- `createConfig(input)` → `LoyaltyProgramConfig`
- `updateConfig(tenantId, input)` → `LoyaltyProgramConfig`
- `adjustPoints(input)` → `ManualPointAdjustmentResult`
- `getAdminCustomerLoyaltySummary(tenantId, customerId)` → `AdminCustomerLoyaltySummary`
- `getAdminCustomerLoyaltyList(tenantId)` → `AdminCustomerLoyaltySummary[]`

## Error Types
- `LoyaltyConfigNotFoundError`: config not found for tenant
- `LoyaltyValidationError`: invalid input or config
- `InsufficientPointsError`: balance too low for redemption/adjustment
- `LoyaltyProgramDisabledError`: program not enabled for tenant

## Validation
- `pnpm --filter @platform/api test` ✅
- Contract tests: 28 assertions covering all 5 contract functions

## Files Changed
- `platform/apps/api/src/loyalty/loyalty-api-contracts.ts` (new)
- `platform/apps/api/src/loyalty/loyalty-api-contracts.test.ts` (new)
- `platform/apps/api/src/loyalty/loyalty.service.ts` (new)
