# Handoff Note

## Header

- Task ID: E3-S3-T3
- Task Title: add preview environment metadata to tenant configuration
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T3.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-environment-metadata.service.ts (new — lifecycle-aware preview metadata derivation)
  - ../../../../platform/apps/api/src/auth/preview-environment-metadata.service.test.ts (new — 17 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered PreviewEnvironmentMetadataService in providers and exports)
- New or updated contracts:
  - `PreviewEnvironmentMetadataService` — NestJS injectable that derives `PreviewEnvironmentMetadata` from tenant status, preview subdomain, and managed domain configuration
  - Builds on existing `buildPreviewEnvironmentMetadata()` from @platform/types but adds tenant lifecycle access gating via `canTenantAccessLifecycleMode(status, "preview-routing")`
  - Inaccessible tenants (suspended, archived) get `not-configured` status and all surfaces marked `available: false` while retaining preview URLs for diagnostic visibility
  - `PreviewEnvironmentMetadataRequest` — service input requiring tenantId, previewSubdomain, and tenantStatus
  - Existing types consumed: `PreviewEnvironmentMetadata`, `PreviewSurfaceMetadata`, `PreviewEnvironmentMetadataOptions`, `PreviewSurfaceType` from @platform/types
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 39 files, 138 tests pass (up from 121)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - active and draft tenants with subdomain produce `configured` environment status
  - suspended and archived tenants produce `not-configured` with surfaces marked unavailable
  - empty subdomain produces `not-configured` with null URLs
  - storefront and admin URLs derived correctly from managed domain config
  - missing domain config produces null URLs and unavailable surfaces
  - inaccessible tenants retain URLs for diagnostic visibility but mark surfaces unavailable
  - contract shape stability: all required fields present in returned metadata
  - provisioning integration: freshly provisioned draft tenant produces correct metadata
- Known untested cases and reason:
  - no Playwright tests — no browser-visible behavior changed
  - Playwright impact: none

## Assumptions and Risks

- Assumptions made:
  - the existing `buildPreviewEnvironmentMetadata()` and `PreviewEnvironmentMetadata` types from @platform/types are the canonical shape for preview metadata
  - tenant lifecycle access mode "preview-routing" is the correct gate for preview environment availability
  - preview URLs should be retained even for inaccessible tenants to support platform admin diagnostic views
- Risks introduced or still open:
  - the service is standalone; downstream frontend bootstrap and operational summary surfaces must wire it in to consume lifecycle-aware metadata
  - managed domain configuration is constructor-injected; runtime configuration wiring deferred to integration tasks
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T4 (safe fallback behavior for unresolved preview routes)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-environment-metadata.service.ts
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - E3-S3-T4 should consume both the PreviewRouteResolutionService (for route resolution) and PreviewEnvironmentMetadataService (for metadata) to define fallback behavior
  - the metadata service returns stable URL strings even for unavailable tenants so fallback pages can suggest correct endpoints
- Board update required:
  - add E3-S3-T3 to Completed section
