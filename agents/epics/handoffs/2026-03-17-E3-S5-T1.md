# Handoff Note

## Header

- Task ID: E3-S5-T1
- Task Title: define module registry and enablement configuration model
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S5-T1.md

## What Changed

- Changed surfaces:
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../platform/packages/types/src/auth.test.ts
  - ../../../../platform/apps/api/src/auth/module-registry.service.ts
  - ../../../../platform/apps/api/src/auth/module-registry.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - Shared auth types now export `moduleCategories`, `ModuleCategory`, `ModuleRegistryEntry`, `moduleRegistry` (record mapping all 5 module keys to their metadata, categories, and required dependencies), `getModuleRegistryEntry()`, `getFullModuleRegistry()`, `isValidModuleKey()`, `getModuleDependencies()`, `ModuleEnablementValidationResult` discriminated union, `validateModuleEnablementSet()`, and `TenantModuleEnablementRecord`.
  - API now exposes `ModuleRegistryService` as a NestJS injectable wrapping the shared helpers, with methods for registry lookup, key validation, dependency resolution, and enablement validation. Also exports `ModuleRegistryError` with typed reasons (`empty-set`, `missing-dependency`, `unknown-module`).
  - Module dependency rules: `catalog` and `content` and `operations` have no dependencies; `ordering` requires `["catalog"]`; `bookings` requires `["catalog"]`.
- New config, env vars, events, or migrations:
  - no new env vars, events, or migrations

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 38 files, 121 tests passed
  - `pnpm --filter @platform/api typecheck` — clean
  - `pnpm typecheck:contracts` — clean
  - `pnpm --filter @platform/types test` — 2 files, 11 tests passed
- Key passing cases:
  - module registry contains exactly 5 entries matching the `tenantModuleKeys` constant
  - `isValidModuleKey` accepts valid keys and rejects unknown strings
  - `validateModuleEnablementSet` accepts dependency-complete combinations (e.g., `["catalog", "ordering"]`) and rejects missing-dependency sets (e.g., `["ordering"]` without `["catalog"]`)
  - empty and unknown-key sets are correctly rejected with appropriate reasons
  - `ModuleRegistryService` wraps shared helpers and throws `ModuleRegistryError` with structured reasons on invalid enablement
- Known untested cases and reason:
  - no persistence-backed module enablement storage was tested because this task only defines the registry contract and validation logic
  - no frontend route gating or capability propagation was tested because those are downstream tasks (E3-S5-T4, E7-S6)

## Assumptions and Risks

- Assumptions made:
  - module keys are derived from the existing `tenantModuleKeys` constant and remain stable for provisioning, frontend route gating, and future publish validation
  - dependency rules are minimal for now (only `ordering` and `bookings` depend on `catalog`); additional constraints can be added to the registry without breaking the contract shape
  - `TenantModuleEnablementRecord` captures per-module enabled state with an optional `enabledAt` timestamp; this shape is sufficient for E3-S5-T4 capability propagation
- Risks introduced or still open:
  - module enablement is not yet enforced at auth or runtime level; downstream tasks must wire `ModuleRegistryService.requireValidEnablement()` into tenant provisioning and configuration flows
  - current environment reports an engine warning because the workspace expects Node `>=22.12.0` but validation ran on Node `v22.11.0`
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E3-S5-T2, E3-S5-T3, E3-S5-T4
- Recommended first files or docs to read:
  - platform/packages/types/src/auth.ts (module registry types and helpers)
  - platform/apps/api/src/auth/module-registry.service.ts (DI-ready service)
  - agents/epics/packets/epic-03/E3-S5-T1.md (this task's packet for context)
- Specific blocker or caution for next agent: module enablement validation only checks dependency completeness; it does not enforce auth, billing, or vertical-template compatibility — those must be layered in downstream tasks
- Board update required: yes — move E3-S5-T1 from Ready to Completed
