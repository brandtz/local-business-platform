# Handoff Note

## Header

- Task ID: E12-S1-T4
- Task Title: Package comparison read model
- Status: completed
- Date: 2026-03-22
- Primary owner lane: Epic 12 Foundation
- Supporting lane: none
- Packet path: ../packets/epic-12/E12-S1-T4.md

## What Changed

- Changed surfaces:
  - platform/packages/types/src/subscription.ts — PackageComparisonModel, PackageComparisonSummary, PackageComparisonRow, FeatureComparisonValue types; buildPackageComparisonModel function
  - platform/apps/api/src/subscription/subscription-package.service.ts — getPackageComparisonModel() method on service
  - platform/apps/api/src/subscription/subscription-package-api-contracts.ts — PackageComparisonResponse type alias

- Comparison read model shape:
  ```typescript
  PackageComparisonModel = {
    packages: PackageComparisonSummary[];  // sorted by displayOrder
    features: PackageComparisonRow[];       // module, usage-limit, premium-feature rows
  }
  
  PackageComparisonSummary = {
    id, name, description, basePriceCents, billingInterval,
    trialDurationDays, displayOrder, featuredBadge?, ctaLabel
  }
  
  PackageComparisonRow = {
    featureName: string;
    featureCategory: "module" | "usage-limit" | "premium-feature";
    values: Record<packageId, { included: boolean; limit?: string; note?: string }>;
  }
  ```

- CTA label logic:
  - Has trial → "Start Free Trial"
  - No trial, price > 0 → "Choose Plan"
  - No trial, price = 0 → "Contact Sales"

- Only active packages included in comparison (deprecated excluded)

## Validation

- Test layers run:
  - Unit: comparison model build with sorting, CTA labels, feature categories — 4 tests
  - Service integration: getPackageComparisonModel excludes deprecated, provides all plan-picker UI data — 4 tests
  - API typecheck: pass

## Open Assumptions

- featuredBadge is optional — set by platform admin separately (not auto-calculated)
- Comparison model built on-demand; caching deferred to performance optimization

## Follow-Up Hooks

- E9-S1 (onboarding wizard plan-picker consumes PackageComparisonModel)
- E12-S6 (tenant admin plan-change flow consumes comparison model)
