# Handoff Note

## Header

- Task ID: E12-S1-T2
- Task Title: Define feature entitlement schema
- Status: completed
- Date: 2026-03-22
- Primary owner lane: Epic 12 Foundation
- Supporting lane: none
- Packet path: ../packets/epic-12/E12-S1-T2.md

## What Changed

- Changed surfaces:
  - platform/apps/api/prisma/schema.prisma — added PackageEntitlement, PackageUsageLimit, PackagePremiumFeature models with enums UsageLimitType, UsageLimitResetPeriod
  - platform/packages/types/src/subscription.ts — added entitlement record types, usage limit types, premium feature flags, entitlement map types, extraction function

- New or updated contracts:
  - PackageEntitlementRecord: { id, createdAt, packageId, moduleKey (string referencing TenantModuleKey), enabled (boolean) } — @@unique([packageId, moduleKey])
  - UsageLimitRecord: { id, createdAt, packageId, limitType (orders_per_month|storage_gb|staff_seats), softLimit (nullable), hardLimit (integer), resetPeriod (monthly|annual|none) }
  - PremiumFeatureRecord: { id, createdAt, packageId, featureFlag (string from premiumFeatureFlags) }
  - PackageEntitlementMap: { modules: Record<string, boolean>, premiumFeatures: PremiumFeatureFlag[], usageLimits: PackageUsageLimitMap }
  - premiumFeatureFlags: advanced-analytics, api-access, custom-branding, dedicated-support, sso, custom-integrations, white-label, priority-support
  - extractEntitlementMap(pkg): PackageEntitlementMap — derives entitlement map from package records

- Entitlement consumption by E3-S5:
  - modules map keys are TenantModuleKey strings from E3-S5
  - E12-S2 can consume PackageEntitlementMap.modules directly for module gating
  - E12-S2 can consume PackageEntitlementMap.premiumFeatures for premium feature gating
  - E12-S2 can consume PackageEntitlementMap.usageLimits for usage enforcement

## Validation

- Test layers run:
  - Unit: type guards for UsageLimitType, PremiumFeatureFlag, extractEntitlementMap — 10+ tests
  - API typecheck: pass
  - Lint: pass (0 warnings)

## Open Assumptions

- Module keys align with E3-S5 TenantModuleKey (catalog, ordering, bookings, content, operations)
- Premium feature flags are extensible — new flags can be added to premiumFeatureFlags array
- Usage limit types are extensible — new types can be added to usageLimitTypes array

## Follow-Up Hooks

- E12-S2 (feature gating consumes entitlement map)
- E12-S4 (subscription lifecycle uses entitlement snapshot)
