# Handoff Note — E11-S1-T2

- Task ID: E11-S1-T2
- Task Title: Analytics Computation Pipeline
- Status: completed
- Date: 2026-03-22

## What Changed

- Changed surfaces:
  - platform/apps/api/src/analytics/analytics.repository.ts — AnalyticsRepository with upsertAggregation (idempotent), getAggregations (tenant-scoped with time/location filtering), createJob, updateJobStatus, getJobById, listJobsForTenant
  - platform/apps/api/src/analytics/analytics.service.ts — AnalyticsService.runAggregation() method: creates job, pulls orders/bookings via data providers, computes metrics (revenue, counts, new/returning customers, channel breakdown), upserts aggregation record, updates job status
- New contracts: OrderDataProvider and BookingDataProvider interfaces for data sourcing

## Computation Pipeline

- runAggregation(input: AggregationJobInput) → RunAggregationResult
- Idempotent: re-running for same (tenantId, periodType, periodStart) overwrites existing aggregation via upsert
- Job lifecycle: pending → running → completed/failed
- Metrics computed: revenueCents (from completed orders), orderCount, bookingCount, newCustomerCount, returningCustomerCount
- Channel attribution: maps OrderFulfillmentMode to analytics channels (delivery, pickup, dine_in, booking)
- Customer analysis: tracks unique customerIds across orders+bookings; customers appearing more than once are "returning"

## Validation

- Test layers run: pnpm --filter @platform/api test (2221 tests pass — 46 new analytics tests)
- Key passing cases: aggregation creates records from completed orders/bookings, idempotent re-runs, empty data handling, channel breakdown computation, customer new/returning tracking

## Open Assumptions

- Data providers are injected via setter methods (not constructor DI) for flexibility
- In-memory repository; production would use Prisma
- Customer "returning" determination is within-period (same period, multiple transactions)

## Follow-Up Hooks

- E11-S1-T3 uses aggregated data for query API
- Prompt 27 (E11-S6) may add subscription channel to aggregation
