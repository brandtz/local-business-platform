# Handoff Note — E7-S6-T1

- Task ID: E7-S6-T1
- Task Title: Operating Mode Rules
- Status: completed
- Date: 2026-03-22
- Primary owner lane: Platform Control Lane
- Supporting lane: API Surface Lane
- Packet path: agents/epics/handoffs/2026-03-22-E7-S6-T1.md

## What Changed

- Changed surfaces:
  - platform/packages/types/src/auth.ts — Added operating mode type definitions: `TenantOperatingMode` ("ordering" | "booking" | "hybrid"), `TransactionFlow` ("cart" | "order" | "booking"), `OperatingModeRules` type, `resolveOperatingMode()`, `getOperatingModeRules()`, `isTransactionFlowAllowed()`, `getBlockedFlows()`, `operatingModes` const, `transactionFlows` const
  - platform/packages/types/src/auth.test.ts — Added 13 tests covering mode resolution, rules retrieval, flow allowance, and blocked flows
- New or updated contracts: Operating mode rules added to @platform/types public contract
- New config, env vars, events, or migrations: None

## Operating Mode Rules by Module Combination

| Enabled Modules | Resolved Mode | Allowed Flows | Blocked Flows |
|---|---|---|---|
| ordering (+ catalog) | ordering | cart, order | booking |
| bookings (+ catalog) | booking | booking | cart, order |
| ordering + bookings (+ catalog) | hybrid | cart, order, booking | none |
| neither ordering nor bookings | ordering (defensive default) | cart, order | booking |

## Key Functions

- `resolveOperatingMode(enabledModules)` — Derives mode from enabled module set
- `getOperatingModeRules(mode)` — Returns full rules object for a mode
- `isTransactionFlowAllowed(mode, flow)` — Boolean check for flow availability
- `getBlockedFlows(mode)` — Returns array of blocked transaction flows

## Validation

- Test layers run: pnpm typecheck:contracts (clean), pnpm --filter @platform/types test (217 tests pass)
- Key passing cases: 13 operating mode tests covering all 3 modes, flow resolution, blocking
- Known untested cases and reason: None

## Assumptions and Risks

- Assumptions made:
  - Operating mode is derived from module enablement, not stored separately
  - The defensive default when neither ordering nor bookings is enabled is "ordering"
  - Modes are exhaustive: exactly three modes cover all module combinations
- Risks: None
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E7-S6-T2 (backend enforcement), E7-S6-T3 (frontend gating)
- Recommended first files: platform/packages/types/src/auth.ts
- Specific blocker or caution: Do not modify the operating mode rules map without updating downstream enforcement and gating
- Board update required: yes
- Playwright impact: none
