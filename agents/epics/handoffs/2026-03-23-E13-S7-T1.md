# E13-S7-T1: Orders Dashboard and List

## Status: Complete

## Files Created
- `platform/apps/web-admin/src/pages/orders-page.ts` — Orders list page component
- `platform/apps/web-admin/src/pages/orders-page.test.ts` — Tests (13 tests)

## Files Modified
- `platform/apps/web-admin/src/routes.ts` — Replaced placeholder with real OrdersPage

## Route
- `/ordering` → OrdersPage

## SDK Methods Consumed
- `orders.list(params?: AdminOrderListQuery)` — Fetch paginated orders with status/search filters
- `orders.updateStatus(id, { status })` — Inline quick actions for status transitions

## Order Workflow State Machine
| Current Status | Available Actions (Inline Buttons) |
|---|---|
| placed | Accept (→ confirmed) |
| confirmed | Start Prep (→ preparing) |
| preparing | Mark Ready (→ ready) |
| ready | Complete (→ completed) |
| completed | (none) |
| cancelled | (none) |

## Implementation Details
- Status filter tabs: All, Pending, Accepted, Preparing, Ready, Completed, Cancelled
- MetricCards row displays pipeline counts (when available)
- SearchToolbar with order # or customer name search
- Pagination with page/pageSize
- Click row → navigates to `/ordering/:orderId`
- Inline action buttons context-sensitive per status

## Test Results
- 13 tests pass covering status tabs, row building, pipeline view, pagination logic
