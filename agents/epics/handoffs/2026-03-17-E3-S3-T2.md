# Handoff Note

## Header

- Task ID: E3-S3-T2
- Task Title: implement routing resolution for preview storefront and admin surfaces
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - New PreviewRouteResolutionService that maps preview hosts to the correct tenant and surface (storefront or admin) with fail-closed semantics
  - Supports separate managed domain lists for storefront preview domains and admin preview domains
  - Enforces preview-routing lifecycle access mode (allows draft and active tenants; denies suspended and archived)
  - Returns a discriminated union result with specific unresolved reasons (unknown-host, no-matching-domain, tenant-not-found, tenant-lifecycle-denied)
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
- Key passing cases:
  - storefront preview hosts resolve to the correct tenant with surface storefront
  - admin preview hosts resolve to the correct tenant with surface admin
  - draft tenants are accessible on both storefront and admin preview domains
  - active tenants are accessible on both storefront and admin preview domains
  - suspended tenants are denied with tenant-lifecycle-denied reason
  - archived tenants are denied with tenant-lifecycle-denied reason
  - unknown subdomains fail closed with tenant-not-found reason
  - non-matching domains fail closed with no-matching-domain reason
  - null, undefined, and empty hosts fail closed with unknown-host reason
  - bare managed domains without subdomain prefix are rejected
  - nested subdomain chains are rejected
  - host normalization (case, port stripping) is applied before resolution
  - when storefront and admin domains overlap, storefront match takes precedence
- Known untested cases and reason:
  - integration with request-context pipeline and middleware is not yet wired
  - Playwright impact: none (no browser-visible route behavior changed in this task)

## Assumptions and Risks

- Assumptions made:
  - separate managed domain lists for storefront and admin preview surfaces is the correct configuration model
  - storefront preview domain match takes precedence over admin when domains overlap
  - the preview-routing lifecycle access mode (draft + active) is the correct access policy for both surfaces
- Risks introduced or still open:
  - the service is not yet wired into the request-context or middleware pipeline; downstream tasks must integrate it
  - admin preview routing may need additional authorization checks beyond lifecycle access in future tasks
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T3
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/request-context.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - preview metadata and frontend bootstrap flows should consume the PreviewRouteResolutionResult to propagate the resolved surface type to client applications
- Board update required:
  - move E3-S3-T2 to Completed with handoff path ./2026-03-17-E3-S3-T2.md
