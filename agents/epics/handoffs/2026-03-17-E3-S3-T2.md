# Handoff Note

## Header

- Task ID: E3-S3-T2
- Task Title: implement routing resolution for preview storefront and admin surfaces
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - API now exposes a PreviewRouteResolutionService that maps preview hosts to the correct tenant and surface (storefront or admin)
  - Separate managed domain lists for storefront and admin preview surfaces allow deterministic surface detection
  - PreviewRouteResolutionResult is a discriminated union with resolved or unresolved variants including explicit failure reasons
  - Fail-closed behavior guaranteed: missing hosts, unmatched domains, unknown tenants, and inaccessible tenant statuses all produce unresolved results with distinct reasons
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
- Key passing cases:
  - storefront preview hosts resolve to correct tenant with surface storefront
  - admin preview hosts resolve to correct tenant with surface admin
  - draft and active tenants pass preview-routing lifecycle access
  - suspended and archived tenants fail closed with tenant-not-accessible reason
  - missing, empty, and null hosts fail closed with no-host reason
  - hosts not matching any managed domain fail closed with no-matching-domain reason
  - preview subdomains not matching any tenant fail closed with tenant-not-found reason
  - tenant isolation verified: distinct preview hosts never cross-resolve to wrong tenant
  - same tenant resolves to distinct surfaces based on host domain
  - host normalization handles casing and port stripping
  - empty domain configuration fails closed
- Known untested cases and reason:
  - integration with real HTTP request pipeline not yet tested because the service is a standalone policy service consumed by future request middleware
  - Playwright impact: none; no browser-visible route behavior changed in an app shell

## Assumptions and Risks

- Assumptions made:
  - separate managed domain lists for storefront and admin surfaces is the correct preview routing pattern (storefront: preview.local, admin: admin.preview.local)
  - preview-routing lifecycle access mode from @platform/types is the correct gate for both storefront and admin preview surfaces
  - admin preview resolution is checked before storefront to prevent a storefront domain from accidentally matching an admin host pattern
- Risks introduced or still open:
  - future request middleware must wire this service into the HTTP request lifecycle to enforce the policy at runtime
  - downstream frontend bootstrap flows must consume the resolved surface to determine which app shell to render
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T3
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - preview metadata and frontend bootstrap flows should consume the PreviewRouteResolutionResult surface field to determine which app shell to render
  - the resolution service is standalone and not yet wired into request middleware
- Board update required:
  - move E3-S3-T2 to Completed
