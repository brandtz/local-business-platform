# Handoff Note

## Header

- Task ID: E3-S3-T2
- Task Title: implement routing resolution for preview storefront and admin surfaces
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts (new)
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.test.ts (new)
  - ../../../../platform/apps/api/src/app.module.ts (updated to register PreviewRouteResolutionService)
- New or updated contracts:
  - PreviewRouteResolutionService resolves preview hosts to a tenant and a surface (storefront or admin) using separate managed domain lists for each surface type
  - PreviewRouteResolutionResult is a discriminated union with kind resolved or unresolved, where unresolved includes a reason (no-host, no-matching-domain, no-matching-tenant)
  - PreviewSurface type is storefront or admin
  - PreviewRouteResolutionOptions accepts managedStorefrontPreviewDomains and managedAdminPreviewDomains independently
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` (106 tests passed, 35 test files)
  - `pnpm --filter @platform/api typecheck` (clean)
- Key passing cases:
  - storefront preview hosts resolve to the correct tenant and storefront surface
  - admin preview hosts resolve to the correct tenant and admin surface
  - missing hosts fail closed with no-host reason
  - hosts outside all managed domains fail closed with no-matching-domain reason
  - valid managed-domain hosts with unknown subdomains fail closed with no-matching-tenant reason
  - bare managed domains without subdomains fail closed
  - nested subdomain chains on managed preview domains are rejected
  - host casing and port normalization produce correct resolution
  - cross-tenant isolation is preserved by exact subdomain matching
  - storefront hosts never resolve to admin surface and vice versa
  - empty options (no managed domains configured) fail closed
- Known untested cases and reason:
  - integration with TenantResolutionService is not yet wired because that coupling belongs in downstream consumer tasks
  - Playwright impact: none (no browser-visible route behavior changes in this task)

## Assumptions and Risks

- Assumptions made:
  - storefront and admin preview surfaces use separate managed domain lists rather than a subdomain prefix convention
  - the PreviewRouteResolutionService is a standalone policy service that can be consumed by TenantResolutionService or middleware in a downstream task
  - preview resolution reuses the same resolveManagedSubdomain helper from tenant-request-host for consistent subdomain extraction
- Risks introduced or still open:
  - future tasks must decide whether to wire this service into TenantResolutionService or invoke it from middleware independently
  - the managedAdminPreviewDomains and managedStorefrontPreviewDomains lists need to be populated from runtime config or environment in a downstream task
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T3 (preview metadata and frontend bootstrap flows)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - the preview route resolution service is not yet integrated into the request pipeline; downstream tasks must wire it into middleware or the existing TenantResolutionService based on their scope
- Board update required:
  - move E3-S3-T2 to Completed with handoff note path ./2026-03-17-E3-S3-T2.md
