# Handoff Note

## Header

- Task ID: E3-S3-T2
- Task Title: implement routing resolution for preview storefront and admin surfaces
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts (new)
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.test.ts (new)
  - ../../../../platform/apps/api/src/app.module.ts (updated providers and exports)
- New or updated contracts:
  - PreviewRouteResolutionService maps preview hosts to the correct tenant and surface (storefront or admin) with fail-closed behavior for missing hosts, unmatched preview domains, and unknown preview subdomains
  - PreviewRouteResolutionResult discriminated union with resolved and unresolved variants
  - PreviewRouteUnresolvedReason enum: missing-host, no-matching-preview-domain, unknown-preview-subdomain
  - PreviewSurface type: storefront or admin
- New config, env vars, events, or migrations:
  - PreviewRouteResolutionOptions accepts managedPreviewStorefrontDomains and managedPreviewAdminDomains configuration arrays

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 104 tests passed (35 files), including 9 new preview route resolution tests
  - `pnpm --filter @platform/api typecheck` — clean
- Key passing cases:
  - storefront preview hosts resolve to the correct tenant with surface storefront
  - admin preview hosts resolve to the correct tenant with surface admin
  - same tenant subdomain on different surfaces produces consistent tenant identity with distinct surface labels
  - missing or empty hosts fail closed with missing-host reason
  - hosts not matching any managed preview domain fail closed with no-matching-preview-domain reason
  - valid preview domain prefix but unknown subdomain fails closed with unknown-preview-subdomain reason
  - bare managed domains without a subdomain prefix are rejected
  - nested subdomain chains are rejected
  - host normalization (case, port, whitespace) is applied before resolution
- Known untested cases and reason:
  - integration with lifecycle access modes is out of scope for this task and should be covered by downstream tasks consuming this service
  - Playwright impact: none — no browser-visible route behavior changes in this task

## Assumptions and Risks

- Assumptions made:
  - storefront and admin preview surfaces use separate managed domain sets (e.g., preview.local vs admin-preview.local) rather than path or port differentiation
  - the PreviewRouteResolutionService is complementary to TenantResolutionService and does not replace it
- Risks introduced or still open:
  - downstream consumers must wire this service into request context and lifecycle access mode enforcement
  - future onboarding, publish, and domain-promotion flows should reference this service for deterministic preview route semantics
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T3 (preview metadata and frontend bootstrap flows)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-policy.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - preview metadata should consume the resolved surface type from PreviewRouteResolutionResult to determine which app shell and bootstrap config to serve
  - lifecycle access enforcement should layer on top using the existing TenantRequestPolicyService with the appropriate access mode for the resolved surface
- Board update required:
  - move E3-S3-T2 to Completed
