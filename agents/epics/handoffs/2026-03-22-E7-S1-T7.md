# Handoff Note — E7-S1-T7

- Task ID: E7-S1-T7
- Task Title: Storefront Cart Integration
- Status: completed
- Date: 2026-03-22
- Primary owner lane: Platform Control Lane
- Supporting lane: Verification Lane
- Packet path: agents/epics/handoffs/2026-03-22-E7-S1-T7.md

## What Changed

- Changed surfaces:
  - platform/apps/web-customer/src/cart-state.ts — CartState type, state functions (createInitialCartState, applyCartResponse, applyStepperData, setCartLoading, setCartError), selectors (getCartItemCount, isCartEmpty, hasStaleItems, getStaleItems), stepper navigation (CheckoutStep, checkoutSteps, canAdvanceToStep)
- New or updated contracts:
  - Server-authority contract: Client NEVER computes prices. All state derived from server responses via applyCartResponse().
  - 3-step stepper data contract:
    - Step 1 (Cart): items[], quote (PricingQuote)
    - Step 2 (Fulfillment): fulfillmentMode, deliveryAddress
    - Step 3 (Payment): subtotal/discount/tax/tip/deliveryFee/total, promoCode, loyaltyCode, orderNotes
  - Stepper navigation rules:
    - Cart → Fulfillment: requires non-empty cart with no stale items
    - Fulfillment → Payment: requires pickup or delivery-with-address
    - Backward navigation always allowed
  - Stale-price UX: hasStaleItems() and getStaleItems() selectors for warning display
- New config, env vars, events, or migrations: None

## Validation

- Test layers run: pnpm --filter web-customer test (497 tests passed), pnpm --filter web-customer typecheck (clean)
- Key passing cases: 26 cart-state tests covering initial state, server response application, loading/error states, item counting, stale item detection, stepper navigation (advance blocking on stale items, empty cart, missing delivery address), backward navigation
- Known untested cases and reason: None

## Playwright Evidence

Cart state is pure logic (no DOM rendering); browser-visible cart components are deferred to checkout implementation.

## Assumptions and Risks

- Assumptions made: Client is a thin state layer that delegates all computation to the server. No client-side price calculation is permitted.
- Risks introduced or still open: None
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E7-S2 (checkout flow), E8-S2 (payment step integration)
- Recommended first files or docs to read: platform/apps/web-customer/src/cart-state.ts
- Specific blocker or caution for next agent: Preserve server-authority invariant — client must never compute prices. Stepper navigation guards must be maintained when adding checkout UI.
- Board update required: yes
