# E8-S1-T3: Tenant-Admin APIs and Forms — Handoff

## Task ID & Status
- **Task**: E8-S1-T3
- **Status**: Completed
- **Date**: 2026-03-22

## Changed Surfaces
- `platform/apps/api/src/payment/payment-connection-api-contracts.ts` — Request validation: `assertValidCreatePaymentConnectionRequest`, `assertValidVerifyPaymentConnectionRequest`, `assertValidDeactivatePaymentConnectionRequest`, `assertNoSecretsInResponse`
- `platform/apps/api/src/payment/payment-connection-api-contracts.test.ts` — 23 tests
- `platform/apps/web-admin/src/payment-connection-management.ts` — Admin UI: status badges, provider display, mode badges, connection list rows, form fields (password-type for secrets), form validation, health indicators
- `platform/apps/web-admin/src/payment-connection-management.test.ts` — 32 tests

## Dependency Outputs
- **API contracts**: Validated request types for create, verify, deactivate payment connections
- **Response guard**: `assertNoSecretsInResponse()` — checks no secret fields in JSON output
- **UI components**: `getConnectionStatusBadge()`, `buildConnectionListRow()`, `getProviderFormFields()`, `validateConnectionForm()`, `getHealthIndicator()`
- **Form fields**: All credential inputs use `type: "password"` to prevent visual exposure

## Secret-Handling Rules
- API contracts validate provider-specific credentials on input (Stripe: publishableKey, secretKey; Square: applicationId, accessToken, locationId)
- Credentials are NEVER echoed in any API response
- `assertNoSecretsInResponse()` checks for forbidden patterns in response JSON
- Form fields use `password` type to mask secret input
- Admin list rows never contain credential data

## Security Test Evidence
- 23 API contract tests: valid requests accepted, invalid rejected, no secrets in responses
- 32 web-admin tests: badges, forms, health indicators, no secret leakage
- `assertNoSecretsInResponse` explicitly checks for `encryptedCredentials`, `credentialsIv`, `credentialsTag`, `secretKey`, `accessToken`, `publishableKey`, `applicationId`

## Validation Commands
```
pnpm --filter @platform/api typecheck  # clean
pnpm --filter @platform/api test       # 1593 tests passed
pnpm --filter web-admin test           # 290 tests passed (32 new)
pnpm lint                              # clean
```

## Follow-Up Hooks
- E8-S1-T4 (health read models) — uses same sanitized response types
- E8-S2 (payment processing) — extends API with payment intent endpoints
