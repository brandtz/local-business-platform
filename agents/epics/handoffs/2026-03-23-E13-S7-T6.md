# E13-S7-T6: Staff Management

## Status: Complete

## Files Created
- `platform/apps/web-admin/src/pages/staff-page.ts` — Staff management page
- `platform/apps/web-admin/src/pages/staff-page.test.ts` — Tests (14 tests)

## Files Modified
- `platform/apps/web-admin/src/routes.ts` — Replaced placeholder with StaffPage

## Route
- `/users/staff` → StaffPage

## SDK Methods Consumed
- `staff.list()` — Fetch all staff members
- `staff.get(staffId)` — Fetch staff profile (lazy load on card click)
- `staff.invite({ email, name, role })` — Send staff invitation
- `staff.getSchedule(staffId)` — Fetch weekly schedule
- `staff.deactivate(staffId)` — Deactivate staff member

## Implementation Details
- Staff CardGrid: avatar/initial, name, role, status badge (Active/Inactive)
- Invite Staff button → modal with name, email, role selector
- Staff detail SlidePanel on card click:
  - Profile: name, email, phone, role, status badge
  - Weekly schedule table (day, start time, end time)
  - Deactivate button (for active staff)
- Lazy loading: detail loaded only on card click

## Test Results
- 14 tests pass covering card mapping, invite validation, schedule window validation, conflict detection, day names
