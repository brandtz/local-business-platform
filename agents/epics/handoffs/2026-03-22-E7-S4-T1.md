# E7-S4-T1: Booking Schema and State Machine

## Status: Completed

## Changed Surfaces
- `platform/packages/types/src/booking.ts` — Added BookingStatus enum, state machine transitions, CancellationPolicy type, evaluateCancellationWindow(), BookingRecord, CreateBookingInput, admin/customer booking types, tracking types, quick actions, calendar view types
- `platform/apps/api/prisma/schema.prisma` — Added BookingStatus enum and Booking model with tenant relation and double-booking prevention indexes

## Dependency Outputs
- **BookingStatus enum**: `requested | confirmed | checked-in | completed | cancelled | no-show`
- **State machine**: `bookingStatusTransitions` — requested→[confirmed,cancelled], confirmed→[checked-in,cancelled,no-show], checked-in→[completed,no-show], terminal: completed, cancelled, no-show
- **Cancellation policy**: `CancellationPolicy` type with `freeWindowHours`, `allowLateCancellation`, `lateCancellationPenalty`. `evaluateCancellationWindow()` returns allowed/denied with late penalty info. Default: 24-hour free window, late cancel allowed.
- **BookingRecord**: Full domain record with all timestamp fields (requestedAt, confirmedAt, checkedInAt, completedAt, cancelledAt, noShowAt)
- **Prisma Booking model**: Indexed on [tenantId,status], [tenantId,customerId], [tenantId,staffId,startTime,endTime], [tenantId,startTime], [tenantId,serviceId]

## Test Evidence
- 49+ type tests pass including state machine, cancellation window, tracking steps, quick actions
- `pnpm --filter @platform/api typecheck` passes
- `pnpm typecheck:contracts` passes

## Open Assumptions
- Cancellation policy is passed per-call rather than stored per-tenant (E8 will add tenant-level policy config)
- No deposit/payment fields added — deferred to E8-S2

## Follow-up Hooks
- E7-S4-T2: Booking services consume state machine and cancellation policy
- E8-S2: Payment abstraction adds deposit fields to BookingRecord
