# Handoff: E8-S6-T2

## Task ID and Title
- **Task ID:** E8-S6-T2
- **Title:** Structured Operational Events
- **Status:** Completed

## Changed Surfaces
- `platform/apps/api/src/integration-alerts/operational-event.repository.ts` — NEW: In-memory event store with filtering and pagination
- `platform/apps/api/src/integration-alerts/operational-event.service.ts` — NEW: Event emission service with auto-classification and escalation
- `platform/apps/api/src/integration-alerts/operational-event.service.test.ts` — NEW: 30 unit tests

## Dependency Outputs

### OperationalEventService
- `emitFailureAlert()`: Generic emission with auto-classification via E8-S6-T1 taxonomy
- `emitPaymentConnectionFailure()`: Typed emission for payment failures
- `emitWebhookProcessingFailure()`: Typed emission for webhook failures
- `emitNotificationDeliveryFailure()`: Typed emission for notification failures
- `emitProviderOutage()`: Typed emission for provider API failures
- All methods auto-apply escalation logic based on occurrence count within time window

### OperationalEventRepository
- `insert()`, `findById()`, `list()` with filtering and pagination
- `countRecentByCategory()` for escalation logic
- `acknowledge()` for operator acknowledgement
- `getSeverityDistribution()`, `getCategoryDistribution()`, `getRecentCritical()`, `countUnacknowledged()` for dashboard metrics

### Query Methods (for E8-S6-T3)
- `listAlerts()`: Returns paginated `OperationalAlertSummary[]` with filtering
- `getAlertDetail()`: Returns full `OperationalAlertDetail` with context and resolution hint
- `acknowledgeAlert()`: Marks alert as acknowledged
- `getDashboardMetrics()`: Returns `OperationalAlertDashboardMetrics`

### Integration Points
- Service instruments failure points without modifying E8-S1 through E8-S4 internals
- Events emitted use the E8-S6-T1 taxonomy (category, severity, resolution hints)
- Occurrence counting per-tenant per-category supports escalation

## Test Evidence
- 30 unit tests: payment emission (3), webhook emission (3), notification emission (3), provider emission (2), storage (3), escalation (2), list queries (6), detail (2), acknowledge (2), dashboard metrics (1), authorization (3 via contracts)
- All pass: `pnpm --filter @platform/api test`

## Validation Commands
```bash
pnpm --filter @platform/api test          # 1926 tests pass (61 new)
pnpm --filter @platform/api typecheck     # clean
```

## Playwright Evidence
- Playwright impact: none

## Open Assumptions
- Events stored in-memory; production deployment will use database persistence
- Escalation uses wall-clock time comparison

## Follow-up Hooks
- E8-S6-T3: Consumes query methods for platform-admin dashboard
- Epic 10: Event schema consumed for telemetry integration
