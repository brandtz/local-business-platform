# Handoff Note — E7-S5-T2

- Task ID: E7-S5-T2
- Task Title: Account History Queries
- Status: completed
- Date: 2026-03-22
- Primary owner lane: API Surface Lane

## What Changed

- Changed surfaces:
  - platform/packages/types/src/customer.ts — CustomerOrderHistoryQuery, CustomerBookingHistoryQuery, PaginatedResult<T>, TenantScopedOrderHistoryRecord, TenantScopedBookingHistoryRecord types
  - platform/apps/api/src/customer/customer.service.ts — CustomerService.getOrderHistory(), CustomerService.getBookingHistory() with tenant + customer isolation filtering
  - platform/apps/api/src/customer/customer-api-contracts.ts — assertCustomerOrderHistoryQuery, assertCustomerBookingHistoryQuery validators
- New or updated contracts: History query types with pagination (page, pageSize) and optional status filter. Results are always filtered by tenantId AND customerId.

## Tenant Isolation Enforcement

- Order history: filters by `tenantId === query.tenantId && customerId === query.customerId`
- Booking history: filters by `tenantId === query.tenantId && customerId === query.customerId`
- Cross-tenant leakage test: Tenant A's orders never appear in Tenant B's query results (verified in integration tests)
- Cross-customer leakage test: Customer A's orders never appear for Customer B within the same tenant

## Consumed Contracts

- E7-S2: CustomerOrderSummary type from order.ts (extended with tenant/customer scoping via TenantScopedOrderHistoryRecord)
- E7-S4: CustomerBookingSummary type from booking.ts (extended with tenant/customer scoping via TenantScopedBookingHistoryRecord)

## Validation

- Test layers run: pnpm --filter @platform/api test (1486 tests pass)
- Key passing cases: Tenant-scoped filtering, cross-tenant isolation, status filtering, pagination
- Playwright impact: None (backend API)

## Follow-Up Hooks

- E7-S5-T7: Account shell integration renders order and booking history with empty/populated states
