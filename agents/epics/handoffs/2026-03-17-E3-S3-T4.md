# Handoff Note

## Header

- Task ID: E3-S3-T4
- Task Title: define safe fallback behavior for unresolved preview routes
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S3-T4.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/preview-fallback-policy.service.ts (new — fallback routing policy for unresolved preview hosts)
  - ../../../../platform/apps/api/src/auth/preview-fallback-policy.service.test.ts (new — 12 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered PreviewFallbackPolicyService)
- New or updated contracts:
  - `PreviewFallbackPolicyService` — maps every `PreviewRouteUnresolvedReason` to a safe `PreviewFallbackResponse`
  - `PreviewFallbackResponse` — `{ kind, httpStatus, retryable, safeMessage }` for each failure mode
  - `no-host` → 400, `no-matching-domain` → 404, `tenant-not-found` → 404, `tenant-not-accessible` → 403
  - `no-matching-domain` and `tenant-not-found` share identical safe messages to prevent tenant enumeration
  - No tenant identifiers, slugs, subdomains, or status details leak through safe messages
  - `getFallbackForResolution()` accepts the full discriminated union and returns null for resolved results
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 40 files, 150 tests pass (up from 138)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - each unresolved reason maps to correct HTTP status and kind
  - every reason produces a non-empty safe message
  - no-matching-domain and tenant-not-found produce identical messages (anti-enumeration)
  - safe messages contain no tenant identifiers, slugs, subdomains, or status details
  - tenant-not-accessible does not reveal suspended/archived/draft status
  - resolved results return null fallback
  - all failure reasons are non-retryable
  - contract shape stability verified for all required fields
- Known untested cases and reason:
  - Playwright impact: none — no browser-visible behavior changed

## Assumptions and Risks

- Assumptions made:
  - 404 for both no-matching-domain and tenant-not-found is the correct anti-enumeration approach
  - all current failure modes are non-retryable; future transient failures (DNS, timeouts) may need a retryable path
  - fallback responses are consumed by future HTTP middleware, not directly by frontend code
- Risks introduced or still open:
  - HTTP middleware that wires these responses into actual HTTP responses is not yet built
  - frontend error boundary rendering must consume the safe message without attempting to parse internal details
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S4-T2 (domain verification workflow)
  - E4-S2-T1 (frontend bootstrap flow — consumes fallback shapes for error states)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/preview-fallback-policy.service.ts
  - ../../../../platform/apps/api/src/auth/preview-route-resolution.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - E3-S3 story is now complete; E3-S4 can proceed with custom domain verification
  - E4-S2 should consume PreviewFallbackResponse shape for frontend error boundary rendering
- Board update required:
  - add E3-S3-T4 to Completed section
