# Handoff Note — E11-S1-T1

- Task ID: E11-S1-T1
- Task Title: Analytics Data Model
- Status: completed
- Date: 2026-03-22

## What Changed

- Changed surfaces:
  - platform/apps/api/prisma/schema.prisma — Added AnalyticsAggregationPeriodEnum (DAILY, WEEKLY, MONTHLY), AggregationJobStatusEnum (PENDING, RUNNING, COMPLETED, FAILED), AnalyticsAggregation model with unique constraint on (tenantId, locationId, periodType, periodStart), AggregationJob model with tenant-scoped indexes, Tenant→AnalyticsAggregation relation
  - platform/packages/types/src/analytics.ts — All analytics domain types: AnalyticsAggregationRecord, ChannelBreakdownEntry, AnalyticsChannelType (delivery, pickup, dine_in, booking), AnalyticsAggregationPeriod (daily, weekly, monthly), AggregationJobRecord, AggregationJobInput, RunAggregationResult
  - platform/packages/types/src/index.ts — Added analytics module re-export
- New or updated contracts: Analytics shared types contract with aggregation data model. Prisma schema with analytics entities.

## Analytics Data Model

- AnalyticsAggregation: tenant-scoped, partitioned by locationId, periodType, periodStart/periodEnd
- Fields: revenueCents, orderCount, bookingCount, newCustomerCount, returningCustomerCount, channelBreakdown (JSON)
- Channel types: delivery, pickup, dine_in, booking
- Upsert semantics: unique on (tenantId, locationId, periodType, periodStart) enables idempotent writes
- AggregationJob: tracks computation job lifecycle (pending → running → completed/failed)

## Validation

- Test layers run: pnpm --filter @platform/api typecheck (clean), pnpm typecheck:contracts (clean), pnpm --filter @platform/types test (527 tests pass — 31 new analytics tests)
- Playwright: npx playwright test --project=web-admin-smoke (3 passed)

## Open Assumptions

- Channel breakdown stored as JSON rather than separate relation for simplicity
- locationId is nullable — null represents tenant-wide aggregation
- Period boundaries are ISO 8601 datetime strings

## Follow-Up Hooks

- E11-S1-T2 consumes this model for aggregation writes
- E11-S1-T3 consumes this model for query reads
- Prompt 27 (E11-S6) may extend channel types for subscription revenue
