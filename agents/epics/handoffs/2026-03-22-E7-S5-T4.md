# Handoff Note — E7-S5-T4

- Task ID: E7-S5-T4
- Task Title: Saved Payment Method Management
- Status: completed
- Date: 2026-03-22
- Primary owner lane: API Surface Lane

## What Changed

- Changed surfaces:
  - platform/packages/types/src/customer.ts — SavedPaymentMethod, AddPaymentMethodInput types
  - platform/apps/api/src/customer/customer.service.ts — addPaymentMethod(), listPaymentMethods(), removePaymentMethod(), setDefaultPaymentMethod()
  - platform/apps/api/src/customer/customer.repository.ts — Payment method storage with default-clearing logic
  - platform/apps/api/src/customer/customer-api-contracts.ts — assertAddPaymentMethodInput with raw card number rejection
- New or updated contracts: Payment method model using gateway tokens only. Raw card number fields (cardNumber, rawCardNumber, number, pan) are explicitly rejected at the API contract level.

## Security: No Raw Card Data

- SavedPaymentMethod type has NO cardNumber, rawCardNumber, number, or pan field
- assertAddPaymentMethodInput throws "Raw card numbers are not accepted" if any of these fields are present in the input
- Only gatewayToken, cardBrand, lastFour, expiryMonth, expiryYear are stored
- PaymentMethodDisplayItem (frontend) excludes gatewayToken — only shows brand, last four, and expiry
- Tests explicitly verify no raw card fields exist in stored data or display items

## Payment Method Model

- Fields: id, customerId, tenantId, gatewayToken, cardBrand, lastFour, expiryMonth, expiryYear, isDefault, createdAt
- Set-default clears the previous default method before setting the new one
- Validation: gatewayToken required, lastFour must be exactly 4 chars, expiryMonth 1-12, expiryYear >= 2020

## Validation

- Test layers run: pnpm --filter @platform/api test (1486 tests pass)
- Key passing cases: Add with token, list, remove, set-default, raw card rejection, invalid lastFour rejection, no raw card in stored data
- Playwright impact: None (backend API)

## Follow-Up Hooks

- E7-S5-T7: Account shell renders payment methods tab
- Checkout: payment method can be selected for order payment
