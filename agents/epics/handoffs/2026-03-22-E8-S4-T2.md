# Handoff Note

## Header

- Task ID: E8-S4-T2
- Task Title: Implement queue-backed notification delivery with template rendering
- Status: completed
- Date: 2026-03-22
- Primary owner lane: API Surface Lane
- Supporting lane: none
- Packet path: ../packets/epic-08/E8-S4-T2.md

## What Changed

- Changed surfaces:
  - platform/apps/api/src/notification/notification-delivery.service.ts — new file
- New or updated contracts:
  - NotificationDeliveryService: processEvent() accepts NotificationEvent, resolves templates per channel, renders content, creates delivery records, enqueues jobs
  - processDeliveryJob(): processes a delivery job through an adapter, handles success/failure/retry
  - NotificationDeliveryStore: in-memory store for delivery records and in-app notifications (Prisma integration deferred)
  - InMemoryNotificationQueue: mock queue for testing (BullMQ adapter in production)
  - resolveTemplate(): tenant-specific overrides > shared defaults
  - Queue semantics: idempotent (skips terminal deliveries), exponential backoff retry, dead-letter for exhausted retries
- New config, env vars, events, or migrations:
  - NotificationRetryPolicy: { maxAttempts: 3, baseDelayMs: 5000, maxDelayMs: 60000 }
  - computeBackoffDelay(): delay = min(baseDelay × 2^(attempt-1), maxDelay)
  - Idempotency: buildNotificationIdempotencyKey(eventId, channel) → "notif:{eventId}:{channel}"

## Validation

- Test layers run:
  - Unit: tests in notification-delivery.service.test.ts
  - Typecheck: pnpm --filter @platform/api typecheck — passes
  - Lint: pnpm --filter @platform/api lint — passes
- Key passing cases:
  - Event processing creates delivery records for all channels with templates
  - Template rendering substitutes payload variables
  - Jobs enqueued to queue
  - Idempotent: skips already-delivered channels
  - Multi-channel job creation in single processEvent call
  - Successful delivery through adapter → "delivered" status
  - Transient failure → retry with backoff → re-enqueue with delay
  - Max retries exhausted → "dead-letter" status
  - Invalid recipient → "bounced" status (no retry)
  - Adapter exceptions caught and mapped to failure categories
- Known untested cases and reason:
  - Real BullMQ integration not tested (mock queue used)
  - Concurrent delivery processing not tested (single-threaded tests)

## Assumptions and Risks

- Assumptions made:
  - Queue interface (NotificationQueue) will be implemented with BullMQ in production
  - InMemoryNotificationQueue is sufficient for unit testing
  - Retry policy is configurable per NotificationDeliveryService instance
- Risks introduced or still open:
  - Real queue persistence and job ordering not yet validated
- Invariants changed: none

## Next Handoff Targets

- Next likely task IDs: E8-S4-T3, E8-S4-T4 (completed in this session)
- Downstream: E8-S6 (Integration failure handling monitors notification delivery failures)
- Recommended first files or docs to read:
  - platform/apps/api/src/notification/notification-delivery.service.ts
- Board update required:
  - Move E8-S4-T2 to Completed
