# Handoff Note

## Header

- Task ID: E3-S5-T3
- Task Title: implement platform APIs to assign and update modules and templates per tenant
- Status: completed
- Date: 2026-03-17
- Primary owner lane: API Surface Lane
- Supporting lane: Platform Control Lane
- Packet path: ../packets/epic-03/E3-S5-T3.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/module-template-assignment.service.ts (new — assignment service)
  - ../../../../platform/apps/api/src/auth/module-template-assignment.service.test.ts (new — 16 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered ModuleTemplateAssignmentService)
- New or updated contracts:
  - `ModuleTemplateAssignmentService` — platform-admin APIs for assignment:
    - `assignModules()`: validates module set and assigns to tenant; requires platform admin
    - `assignTemplate()`: validates template and creates association; prevents same-template reassignment
    - `assignModulesWithTemplateCheck()`: combined validation — module enablement + template compatibility
  - `ModuleAssignmentResult` — `{kind: "assigned", tenantId, enabledModules}`
  - `TemplateAssignmentResult` — `{kind: "assigned", association: TenantTemplateAssociation}`
  - `ModuleTemplateAssignmentError` with reasons: `not-authorized`, `invalid-modules`, `incompatible-modules`, `same-template`
  - `AssignmentActorContext` — `{actorType, platformRole}` for authorization checking
  - Authorization: only `platform` actors with `owner` or `admin` role can perform assignments
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 45 files, 232 tests pass (up from 216)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - module assignment accepted for valid sets from platform admin and owner
  - module assignment rejected from tenant actors and anonymous actors
  - invalid module keys, empty sets, and missing dependencies rejected
  - template assignment creates association with correct shape
  - same-template reassignment rejected with structured error
  - different-template reassignment allowed
  - combined module+template check rejects incompatible sets
  - combined check rejects from non-platform-admin
  - contract shape stability for both assignment result types
- Known untested cases and reason:
  - no HTTP controller integration — assignment service is callable but not yet wired to controller endpoints
  - no persistence — returns assignment decisions but does not store them
  - Playwright impact: none

## Assumptions and Risks

- Assumptions made:
  - platform-admin authorization is checked at the service level via `AssignmentActorContext`
  - module validation delegates to `ModuleRegistryService.requireValidEnablement()` which checks keys, dependencies, and non-empty
  - template compatibility delegates to `TemplateRegistryService.requireModuleCompatibility()`
- Risks introduced or still open:
  - no persistence layer; assignments are returned but not stored
  - controller/HTTP endpoint wiring deferred to later integration tasks
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S5-T4 (frontend route gating / capability propagation to runtime)
  - E5-S2 (business profile configuration)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/module-template-assignment.service.ts
  - ../../../../platform/apps/api/src/auth/template-registry.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - E3-S5-T4 should consume module enablement results to determine which frontend routes/capabilities are available
  - the `AssignmentActorContext` type is the auth boundary — downstream controllers must populate it from request context
- Board update required:
  - add E3-S5-T3 to Completed section
