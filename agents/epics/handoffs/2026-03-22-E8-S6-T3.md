# Handoff: E8-S6-T3

## Task ID and Title
- **Task ID:** E8-S6-T3
- **Title:** Platform Views for Failures
- **Status:** Completed

## Changed Surfaces
- `platform/apps/api/src/integration-alerts/alert-api-contracts.ts` — NEW: API contract validation and security assertions
- `platform/apps/api/src/integration-alerts/alert-api-contracts.test.ts` — NEW: 22 unit tests
- `platform/apps/web-platform-admin/src/integration-failure-dashboard.ts` — NEW: Platform-admin failure dashboard views
- `platform/apps/web-platform-admin/src/integration-failure-dashboard.test.ts` — NEW: 23 unit tests

## Dependency Outputs

### API Contracts
- `validateAlertListQuery()`: Validates query parameters (category, severity, tenantId, acknowledged, date range, pagination)
- `assertNoSecretsInAlertView()`: Security check for forbidden fields in view data
- `assertPlatformAdminRole()`: Authorization check

### Platform-Admin Dashboard Views
- `FailureDashboardViewState`: Union type (loading | ready | error)
- `AlertDetailViewState`: Union type for alert detail
- `FailureDashboardFilters`: Category, severity, tenant, acknowledged filters
- `AlertEventRow`: Summary display row with labels and color classes
- `AlertDetailView`: Full detail with context, retry info, resolution hint
- `DashboardMetricsView`: Aggregated severity counts and recent critical alerts

### Display Helpers
- `getCategoryLabel()`, `getSeverityLabel()`, `getSeverityColorClass()`, `getCategoryIconClass()`
- `buildAlertEventRow()`, `buildAlertDetailView()`, `buildDashboardMetricsView()`
- `describeFailureDashboardState()`, `describeAlertDetailState()`

### Security
- NEVER exposes: encryptedCredentials, credentialsIv, credentialsTag, secretKey, accessToken, publishableKey, webhookSecret, signatureKey, cardNumber, rawCardNumber, pan, email, phone, address, ssn, dateOfBirth
- Platform-admin role required

## Test Evidence
- 22 API contract tests: validation (13), security assertions (5), authorization (4)
- 23 platform-admin view tests: state descriptions (5), filters (1), labels (8), row building (2), detail building (2), metrics (1), security (4)
- All pass: `pnpm --filter @platform/api test`, `pnpm --filter web-platform-admin test`

## Validation Commands
```bash
pnpm --filter @platform/api test               # 1926 tests pass
pnpm --filter web-platform-admin test           # 109 tests pass (23 new)
pnpm typecheck:contracts                         # clean
```

## Playwright Evidence
- Playwright impact: YES (platform-admin dashboard)
- Dashboard is data-driven TypeScript with view state pattern — consistent with existing webhook-inspection.ts and payment-health-dashboard.ts patterns
- E2E coverage deferred to integration with actual platform-admin routing

## Open Assumptions
- Dashboard follows existing platform-admin view state pattern (loading | ready | error)
- Pagination defaults: limit 50, offset 0

## Follow-up Hooks
- E8-S6-T4: Dashboard views referenced in operator runbook
- Epic 10: Dashboard patterns available for telemetry observability views
