# Handoff Note

## Header

- Task ID: E2-S4-T2
- Task Title: inject trusted tenant context into request lifecycle and downstream services
- Status: completed
- Date: 2026-03-16
- Primary owner lane: Identity and Security Lane
- Supporting lane: Foundation Lane
- Packet path: ../packets/epic-02/E2-S4-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../platform/packages/types/src/auth.test.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-host.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-host.test.ts
  - ../../../../platform/apps/api/src/auth/request-context.service.ts
  - ../../../../platform/apps/api/src/auth/request-context.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - backend request viewer context now has a dedicated minimal contract separate from frontend auth viewer state
  - request host parsing now prefers forwarded host headers over direct host headers
  - API request context now standardizes route space, trusted viewer identity, and tenant resolution into one downstream object with carrier helpers and a fail-closed `requireTenant` path
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/types test`
  - `pnpm --filter @platform/types typecheck`
  - `pnpm --filter @platform/types lint`
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
  - `pnpm --filter @platform/api lint`
- Key passing cases:
  - backend request viewer helpers preserve minimal trusted identity fields and drop platform role for non-platform viewers
  - forwarded host precedence, tenant-admin context resolution, request-context attachment, and tenant-required downstream reads are covered by unit tests
  - API workspace remains green after adding the request-context service to the Nest module
- Known untested cases and reason:
  - no real controller is consuming the request-context service yet because controller entry-point enforcement belongs to E2-S4-T3
  - no live middleware attaches request context in Nest runtime yet because the repo still lacks authenticated request pipelines and tenant directory loading infrastructure

## Assumptions and Risks

- Assumptions made:
  - `x-forwarded-host` should be treated as the preferred host signal for proxied deployments once trusted proxy policy is defined at the edge
  - downstream tenant-scoped services should consume only `TenantRequestContext` rather than mixing request context with raw host or tenant-id inputs
- Risks introduced or still open:
  - T3 must ensure controller entry points and query helpers actually read the attached request context instead of reconstructing tenant scope independently
  - T4 still needs to define explicit behavior for unresolved, suspended, and archived tenants beyond the current generic fail-closed tenant requirement path
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E2-S4-T3
  - E2-S4-T4
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/request-context.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - ../../../../platform/docs/access-control-model.md
  - ../../../../agents/api/api-spec.md
  - this handoff note
- Specific blocker or caution for next agent:
  - do not let controllers or repository helpers accept raw tenant IDs from callers when a request context is already present; request context must remain the source of truth
- Board update required:
  - move E2-S4-T2 to Completed and either clear the active claim or immediately claim E2-S4-T3