# Handoff Note — E6-S4-T6

- Task ID: E6-S4-T6
- Task Title: Template Rendering Hooks
- Status: completed
- Date: 2026-03-21
- Primary owner lane: Platform Control Lane
- Supporting lane: API Surface Lane
- Packet path: agents/epics/packets/epic-06/E6-S4-T4.md

## What Changed

- Changed surfaces: platform/apps/api/src/content/content-template-hooks.ts (new), platform/apps/api/src/content/content-template-hooks.test.ts (new)
- New or updated contracts:
  - templateRegions — const array of known template regions: hero, about, services, contact, policies, faq, footer
  - TemplateRegion — union type from templateRegions array
  - ContentReadinessIssue — { region, severity, message } for readiness check results
  - ContentReadinessResult — { ready, issues } for publish readiness validation
  - StorefrontPreviewData — { pages, missingRegions } for storefront preview generation
  - ContentTemplateHooksService:
    - buildPublishEvent(tenantId, page) — creates DomainEventEnvelope<ContentPageEventPayload> for publish
    - buildArchiveEvent(tenantId, page) — creates DomainEventEnvelope<ContentPageEventPayload> for archive
    - checkContentReadiness(pages, tenantId, requiredRegions) — validates required regions have published content
    - buildStorefrontPreview(pages, tenantId, expectedRegions) — maps published pages to storefront representation with missing region detection
    - toStorefrontPage(page) — maps single published ContentPageRecord to StorefrontContentPage (returns null for non-published)
    - getEventNameForTransition(from, to) — returns domain event name for status transitions
    - getTemplateRegions() — returns known template regions
- New config, env vars, events, or migrations: Uses existing domainEvents.contentPagePublished and domainEvents.contentPageArchived from @platform/types

## Validation

- Test layers run: pnpm --filter @platform/api test (966 tests passing), pnpm typecheck:contracts (passing)
- Key passing cases:
  - buildPublishEvent creates correct envelope with contentPagePublished event name, tenant, page data
  - buildArchiveEvent creates correct envelope with contentPageArchived event name
  - checkContentReadiness reports ready when all required regions covered, reports issues for missing regions, ignores draft pages and other tenants
  - buildStorefrontPreview maps published pages to storefront format, identifies missing regions, excludes drafts and other tenants
  - toStorefrontPage maps published pages, returns null for draft/archived
  - getEventNameForTransition returns correct events for publish/archive transitions, null for unpublish
- Known untested cases and reason: Actual event bus dispatch (event emission is a downstream concern for E7+); vertical-specific required region lists (E6-S5 vertical template defaults will define these)

## Assumptions and Risks

- Assumptions made: Template regions are a fixed set that can be extended; readiness check uses error severity for missing required regions; preview data includes all published pages regardless of region assignment; event building uses current timestamp via new Date()
- Risks introduced or still open: Template region list may need to be configurable per vertical (E6-S5 concern)
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E6-S5 (vertical template defaults — maps required content regions per vertical), E6-S6 (domain contract stabilization)
- Recommended first files or docs to read: platform/apps/api/src/content/content-template-hooks.ts, platform/packages/types/src/domain-events.ts, platform/apps/api/src/vertical/vertical-template.service.ts
- Specific blocker or caution for next agent: Template-consumption points: ContentTemplateHooksService.checkContentReadiness() accepts requiredRegions parameter — E6-S5 should define which regions are required per vertical; buildStorefrontPreview() provides the preview data shape for storefront rendering
- Board update required: yes
