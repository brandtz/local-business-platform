# Handoff Note

## Header

- Task ID: E3-S5-T4
- Task Title: propagate module configuration into frontend and backend capability checks
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S5-T4.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/module-capability.service.ts (new — capability check service)
  - ../../../../platform/apps/api/src/auth/module-capability.service.test.ts (new — 14 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered ModuleCapabilityService)
  - ../../../../platform/apps/api/src/platform-tenants.controller.ts (fixed — aligned template assignment payload with shared type)
- New or updated contracts:
  - `ModuleCapabilityService` — tenant-scoped module capability checks:
    - `checkAccess()`: returns discriminated result `{allowed: true/false, moduleKey, reason?}`
    - `requireAccess()`: throws `ModuleCapabilityError` if module not enabled
    - `requireAllAccess()`: validates multiple modules at once
    - `getEnabledModules()`: returns the enabled module list from context
    - `buildCapabilityMap()`: returns `Record<TenantModuleKey, boolean>` for frontend consumption
  - `ModuleCapabilityContext` — `{tenantId, enabledModules}` input for all checks
  - `ModuleCapabilityError` with reasons: `module-disabled`, `unknown-module`
  - `ModuleCapabilityCheckResult` — discriminated union for query-style usage
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 46 files, 246 tests pass (up from 232)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - enabled module access allowed
  - disabled module access denied with "module-disabled" reason
  - unknown module key denied with "unknown-module" reason
  - tenant-specific enablement respected (different tenants, different modules)
  - requireAccess throws structured error for disabled/unknown
  - requireAllAccess validates multiple modules
  - capability map returns boolean per module key
  - empty enablement returns all-false capability map
  - all module keys present in capability map
  - tenant isolation: independent checks per tenant context
- Known untested cases and reason:
  - no middleware/guard integration — capability service is injectable but guard wiring deferred to E7-S6
  - no frontend route gating test — frontend composable deferred; `buildCapabilityMap()` provides the payload
  - Playwright impact: none

## Assumptions and Risks

- Assumptions made:
  - capability checks derive from tenant-scoped `ModuleCapabilityContext` provided by upstream request resolution
  - `buildCapabilityMap()` produces the serializable payload for frontend route gating
  - guard-style (`requireAccess`) and query-style (`checkAccess`) are both available for different use cases
- Risks introduced or still open:
  - no middleware integration yet — capability checks are callable but not automatically enforced per-route
  - frontend route gating requires wiring `buildCapabilityMap()` into app shell; deferred to E4/E7
- Invariants changed: no
- Additional fix: aligned `platform-tenants.controller.ts` template assignment to use `payload.templateKey` matching `TenantTemplateAssignmentRequest` shared type

## Next Handoff Targets

- Next likely task IDs:
  - E3-S6-T2 (persistence-backed tenant query)
  - E7-S6 (hybrid operating modes — downstream consumer)
  - E6-S1 (catalog domain model)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/module-capability.service.ts
  - ../../../../platform/apps/api/src/auth/module-template-assignment.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - capability map shape is `Record<TenantModuleKey, boolean>` — frontend should consume this directly for route conditional logic
  - guard/middleware auto-enforcement is not wired yet — controllers must explicitly call `requireAccess()` until guards are added
- Board update required:
  - add E3-S5-T4 to Completed section
