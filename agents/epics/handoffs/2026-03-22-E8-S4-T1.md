# Handoff Note

## Header

- Task ID: E8-S4-T1
- Task Title: Define notification event, template, and channel models
- Status: completed
- Date: 2026-03-22
- Primary owner lane: API Surface Lane
- Supporting lane: none
- Packet path: ../packets/epic-08/E8-S4-T1.md

## What Changed

- Changed surfaces:
  - platform/packages/types/src/notification.ts — new module
  - platform/packages/types/src/index.ts — added notification export
  - platform/apps/api/prisma/schema.prisma — added NotificationTemplate, NotificationDelivery, InAppNotification models
- New or updated contracts:
  - DeliveryChannel enum: "email", "sms", "in-app"
  - NotificationEventType: 8 event types (order.confirmed, order.ready, order.cancelled, booking.confirmed, booking.cancelled, booking.reminder, payment.received, payment.refunded)
  - DeliveryStatus: pending, queued, sending, delivered, failed, bounced, dead-letter
  - FailureCategory: transient, invalid-recipient, provider-error, rate-limited, content-rejected, configuration-error, unknown
  - NotificationEvent, NotificationTemplate, NotificationDeliveryRecord, InAppNotification type contracts
  - DefaultTemplateSeed with 9 default templates (3 event types × 3 channels)
  - Note: renamed from NotificationChannel to DeliveryChannel to avoid conflict with customer.ts NotificationChannel
- New config, env vars, events, or migrations:
  - Prisma enums: NotificationChannel (EMAIL, SMS, IN_APP), DeliveryStatus
  - Prisma models: NotificationTemplate, NotificationDelivery, InAppNotification

## Validation

- Test layers run:
  - Unit: 37 tests in packages/types/src/notification.test.ts
  - Typecheck: pnpm --filter @platform/types typecheck — passes
  - Lint: pnpm --filter @platform/types lint — passes
- Key passing cases:
  - Channel enum coverage (email, sms, in-app)
  - Event type enum coverage (8 types)
  - Template rendering with variable substitution
  - Backoff delay computation (exponential with cap)
  - Idempotency key generation
  - Default template seeding for order.confirmed, booking.confirmed, payment.received
  - Type shape assertions for all domain models
- Known untested cases and reason:
  - Prisma migration not run (no live database in test)
  - Prisma seed script not yet created (deferred to integration)

## Assumptions and Risks

- Assumptions made:
  - DeliveryChannel is the delivery-layer type; customer.ts NotificationChannel remains for preference settings
  - Default templates are shared (tenantId = null); tenant-specific overrides use unique constraint [tenantId, eventType, channel]
- Risks introduced or still open:
  - Template variable names are convention-based (not schema-enforced)
- Invariants changed: none

## Next Handoff Targets

- Next likely task IDs: E8-S4-T2, E8-S4-T3, E8-S4-T4 (all completed in this session)
- Recommended first files or docs to read:
  - platform/packages/types/src/notification.ts
  - platform/apps/api/prisma/schema.prisma (notification section)
- Board update required:
  - Move E8-S4-T1 to Completed
