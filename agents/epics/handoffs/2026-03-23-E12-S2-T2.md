# Handoff Note: E12-S2-T2

## Task ID and Status
- **Task:** E12-S2-T2 — Backend Feature-Gate Middleware
- **Status:** ✅ Complete
- **Date:** 2026-03-23

## What Changed

### New Types (packages/types/src/subscription.ts)
- `FeatureGateDenialReason` — module-not-entitled | premium-feature-not-entitled | usage-limit-exceeded | subscription-expired | subscription-canceled | grace-period-read-only
- `FeatureGateDenial` — structured denial payload with `denied: true`, reason, requiredFeature, currentPackage, upgradePrompt
- `UpgradePromptPayload` — message, suggestedPackageId, suggestedPackageName, ctaLabel, ctaUrl

### New Service (apps/api/src/subscription/feature-gate.service.ts)
- `FeatureGateService` — middleware-style feature gate checks at API boundary
- `FeatureGateError` — error class with `denial: FeatureGateDenial` and `statusCode: 403`
- `checkModuleAccess(subscription, moduleKey)` — returns `FeatureGateDenial | null`
- `checkPremiumFeatureAccess(subscription, featureFlag)` — returns `FeatureGateDenial | null`
- `checkUsageLimitAccess(subscription, limitType, currentUsage)` — returns `FeatureGateDenial | null`
- `requireModuleAccess()` — throws `FeatureGateError` if denied
- `requirePremiumFeatureAccess()` — throws `FeatureGateError` if denied
- `requireUsageLimitAccess()` — throws `FeatureGateError` if denied

## Backend Denial Response Schema (HTTP 403)

```json
{
  "denied": true,
  "reason": "module-not-entitled",
  "requiredFeature": "bookings",
  "currentPackage": "Starter",
  "upgradePrompt": {
    "message": "Upgrade your plan to access the bookings module.",
    "suggestedPackageId": null,
    "suggestedPackageName": null,
    "ctaLabel": "Upgrade Plan",
    "ctaUrl": "/settings/billing/upgrade"
  }
}
```

## Security: Cannot Bypass Feature Gate
- Feature gating is enforced at the service level via `requireModuleAccess()` etc.
- Direct API calls to restricted endpoints receive HTTP 403 with structured denial
- Tests verify bypass resistance for modules, premium features, and usage limits

## Files Changed
- `platform/packages/types/src/subscription.ts` — denial types
- `platform/apps/api/src/subscription/feature-gate.service.ts` — new service
- `platform/apps/api/src/subscription/feature-gate.service.test.ts` — 28 tests

## Validation
- `pnpm --filter @platform/api typecheck` ✅
- `pnpm --filter @platform/api test` — 2506 tests passed ✅

## Downstream Consumers
- E12-S4 (Prompt 31): Subscription lifecycle changes re-evaluate feature gates
- E12-S6 (Prompt 35): Tenant admin billing shows upgrade prompts based on denial payloads
