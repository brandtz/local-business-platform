# Handoff Note

## Header

- Task ID: E3-S4-T4
- Task Title: expose platform-admin and tenant-admin read models for domain status
- Status: completed
- Date: 2026-03-17
- Primary owner lane: API Surface Lane
- Supporting lane: Platform Control Lane
- Packet path: ../packets/epic-03/E3-S4-T4.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/custom-domain-status-query.service.ts (new — read model query service)
  - ../../../../platform/apps/api/src/auth/custom-domain-status-query.service.test.ts (new — 12 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered CustomDomainStatusQueryService)
- New or updated contracts:
  - `CustomDomainStatusQueryService` — exposes domain status read models:
    - `getTenantDomainStatus()`: returns domains for a single tenant, filtering out other tenants' records (tenant-admin scope)
    - `getPlatformDomainStatus()`: returns domain summaries across multiple tenants (platform-admin scope)
  - `CustomDomainStatusSummary` — read-only shape: `hostname`, `id`, `promotionState`, `promotionFailureReason`, `tenantId`, `verificationState`, `verificationFailureReason`
  - `TenantDomainStatusResult` — `{tenantId, domains}`
  - `PlatformDomainStatusResult` — `{tenants: TenantDomainStatusResult[]}`
  - Summary shape intentionally excludes internal timestamps (`createdAt`, `updatedAt`, `promotionStateChangedAt`) and verification evidence for security
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 43 files, 200 tests pass (up from 188)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - tenant-admin: returns only the requesting tenant's domains
  - tenant-admin: filters out other tenants' domain records
  - tenant-admin: returns empty array when no records match
  - platform-admin: returns summaries for multiple tenants
  - platform-admin: isolates records per tenant — no cross-tenant leakage
  - verification and promotion states exposed in summary
  - failure reasons included when present, normalized to null when absent
  - contract stability: summary shape has all required fields
  - contract stability: internal timestamps and evidence not exposed
- Known untested cases and reason:
  - no controller-level authorization gating tested — controller integration deferred to E5/E8 when HTTP layer is wired
  - Playwright impact: none

## Assumptions and Risks

- Assumptions made:
  - the read model is a pure query layer; it does not trigger any state changes
  - consumer is responsible for authorization before calling the service (platform-admin check for cross-tenant, tenant context for single-tenant)
  - internal timestamps and verification evidence are excluded from the summary shape for security and simplicity
- Risks introduced or still open:
  - controller-level HTTP wiring and authorization middleware are not yet integrated
  - summary shape may need to expand when frontend domain management UIs are built
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S5-T2 (enablement persistence for template registry)
  - E5-S2 (business profile configuration — downstream consumer)
  - E8-S5 (domain activation UIs — downstream consumer)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/custom-domain-status-query.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - the query service is pure read model — any state mutations must go through verification/promotion workflow services
  - authorization gating must be enforced at the controller/middleware layer, not in the query service
- Board update required:
  - add E3-S4-T4 to Completed section
