# E8-S1-T4: Sanitized Health Read Models — Handoff

## Task ID & Status
- **Task**: E8-S1-T4
- **Status**: Completed
- **Date**: 2026-03-22

## Changed Surfaces
- `platform/packages/types/src/payment.ts` — Health view types: `PaymentConnectionHealthView`, `PlatformPaymentHealthSummary`, `PlatformPaymentProviderHealth`, `PlatformTenantPaymentHealth`; utility functions: `buildConnectionHealthView()`, `buildPlatformPaymentHealthSummary()`
- `platform/apps/api/src/payment/payment-connection.service.ts` — Health methods: `getTenantConnectionHealth()`, `getPlatformPaymentHealth()`, `getPlatformTenantPaymentHealth()`
- `platform/apps/web-platform-admin/src/payment-health-dashboard.ts` — Platform admin views: `buildPaymentHealthOverview()`, `buildProviderHealthRows()`, `buildTenantHealthRows()`, `assertNoSecretsInHealthView()`
- `platform/apps/web-platform-admin/src/payment-health-dashboard.test.ts` — 19 tests

## Dependency Outputs
- **Tenant health view**: `PaymentConnectionHealthView` — id, provider, displayName, status, mode, lastVerifiedAt, isHealthy (boolean)
- **Platform health summary**: `PlatformPaymentHealthSummary` — total/active/suspended/verifying/inactive counts, per-provider breakdown
- **Platform tenant health**: `PlatformTenantPaymentHealth` — per-tenant connection health list
- **Dashboard components**: `PaymentHealthOverview` (percentage + color), `ProviderHealthRow` (per-provider stats), `TenantHealthRow` (per-tenant summary)

## Secret-Handling Rules
- Health views are constructed from `buildConnectionHealthView()` which explicitly picks only safe fields
- `assertNoSecretsInHealthView()` validates no credential data in view objects
- Platform summary uses only aggregate counts — no individual credentials
- Tests verify JSON serialization of all views contains no secret patterns

## Security Test Evidence
- 19 platform-admin dashboard tests: overview calculation, provider breakdown, tenant grouping, no secrets
- 32 types-package tests: sanitization, health views, platform health summary
- Service-level health tests (in service.test.ts): tenant isolation, no credential leakage in health responses
- `assertNoSecretsInHealthView()` checks for 6 forbidden patterns

## Validation Commands
```
pnpm --filter @platform/api typecheck      # clean
pnpm typecheck:contracts                   # clean
pnpm --filter @platform/api test           # 1593 tests passed
pnpm --filter web-platform-admin test      # 66 tests passed (19 new)
pnpm lint                                  # clean
```

## Follow-Up Hooks
- E8-S2 (payment processing) — extends health views with transaction metrics
- E8-S6 (integration failure handling) — uses health models for operational alerts
