# Handoff Note

## Header

- Task ID: E3-S5-T2
- Task Title: define template registry metadata and tenant-template association rules
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Platform Control Lane
- Supporting lane: Frontend Systems Lane
- Packet path: ../packets/epic-03/E3-S5-T2.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/template-registry.service.ts (new — template registry service)
  - ../../../../platform/apps/api/src/auth/template-registry.service.test.ts (new — 16 tests)
  - ../../../../platform/apps/api/src/app.module.ts (updated — registered TemplateRegistryService)
- New or updated contracts:
  - `TemplateRegistryService` — enriched template registry with:
    - `getEntry()`, `getAllEntries()`, `getAllTemplateKeys()`: template lookup and enumeration
    - `checkModuleCompatibility()`: checks if a module set satisfies a template's required modules
    - `requireModuleCompatibility()`: throws `TemplateRegistryError` if modules are incompatible (includes module enablement validation)
    - `buildAssociation()`: creates `TenantTemplateAssociation` record
    - `canReassign()`: checks if template change is allowed (different template required)
  - `TemplateRegistryEntry` — rich template metadata: `verticalTemplate`, `requiredModules`, `configurationDefaults`, `metadata` (displayName, description, icon)
  - `TemplateModuleCompatibilityResult` — discriminated union: `{compatible: true}` or `{compatible: false, missingModules}`
  - `TenantTemplateAssociation` — tenant-template binding: `{tenantId, verticalTemplate, assignedAt}`
  - `TemplateRegistryError` with reasons: `incompatible-modules`, `invalid-template`, `template-already-assigned`
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test` — 44 files, 216 tests pass (up from 200)
  - `pnpm --filter @platform/api typecheck` — zero errors
  - `pnpm typecheck:contracts` — zero errors
- Key passing cases:
  - registry contains entry for every vertical template key
  - every entry has display metadata and required modules
  - entry shape stability: all required fields present
  - restaurant-core compatible with its required modules
  - restaurant-core compatible when extra modules present
  - incompatible when required modules missing (reports missing list)
  - services-core requires bookings not ordering
  - hybrid-local-business requires all modules
  - requireModuleCompatibility throws for incompatible and invalid module sets
  - requireModuleCompatibility succeeds for valid compatible sets
  - association shape has correct fields
  - canReassign: true for different templates, false for same template
- Known untested cases and reason:
  - no persistence-backed template assignment storage — deferred to E3-S5-T3
  - no auth-level enforcement of template assignment — downstream task
  - Playwright impact: none

## Assumptions and Risks

- Assumptions made:
  - template keys derive from the existing `tenantVerticalTemplateKeys` constant and remain stable
  - template required modules align with the existing `TenantProvisioningTemplateService` profiles
  - display metadata (icon, description) is for frontend consumption; values are stable but may be refined later
- Risks introduced or still open:
  - no persistence layer for template associations; `buildAssociation()` returns records but doesn't store them
  - template reassignment needs downstream policy enforcement (billing, migration)
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs:
  - E3-S5-T3 (capability propagation)
  - E3-S5-T4 (frontend route gating)
  - E6-S5 (vertical template defaults)
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/template-registry.service.ts
  - ../../../../platform/apps/api/src/auth/module-registry.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - template-module compatibility validation is available via `requireModuleCompatibility()` — downstream tasks should use it when changing module enablement
  - `canReassign()` checks identity only; actual template migration policy is out of scope for this task
- Board update required:
  - add E3-S5-T2 to Completed section
