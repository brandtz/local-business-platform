# E13-S7-T2: Order Detail

## Status: Complete

## Files Created
- `platform/apps/web-admin/src/pages/order-detail-page.ts` — Order detail page component
- `platform/apps/web-admin/src/pages/order-detail-page.test.ts` — Tests (18 tests)

## Files Modified
- `platform/apps/web-admin/src/routes.ts` — Added `/ordering/:orderId` route

## Route
- `/ordering/:orderId` → OrderDetailPage

## SDK Methods Consumed
- `orders.get(orderId)` — Fetch full order detail
- `orders.updateStatus(orderId, { status })` — Status workflow buttons
- `orders.refund(orderId, { amountCents, reason })` — Refund form

## Order Workflow State Machine (Buttons Shown)
| Current Status | Forward Actions | Cancel Available |
|---|---|---|
| placed | Confirm Order | Yes |
| confirmed | Start Prep | Yes |
| preparing | Mark Ready | Yes |
| ready | Complete | No |
| completed | (none) | No |
| cancelled | (none) | No |

## Implementation Details
- Order header with status badge, customer name, time ago
- Context-sensitive workflow buttons (from `getOrderQuickActions`)
- Line items table with modifiers, quantities, prices
- Pricing breakdown: subtotal, discount, tax, tip, delivery fee, total
- Fulfillment info: delivery address or pickup
- Customer info card
- Refund section: toggle form with amount + reason
- Internal notes textarea
- Back to Orders navigation

## View Model
Uses `buildOrderDetailViewModel()` from `order-management.ts` to transform
`AdminOrderDetail` into a display-ready view model.

## Test Results
- 18 tests pass covering view model building, workflow buttons, cancellation rules, pricing, address formatting
