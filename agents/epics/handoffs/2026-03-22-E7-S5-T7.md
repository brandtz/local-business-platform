# Handoff Note — E7-S5-T7

- Task ID: E7-S5-T7
- Task Title: Account Shell Integration
- Status: completed
- Date: 2026-03-22
- Primary owner lane: Frontend Systems Lane

## What Changed

- Changed surfaces:
  - platform/apps/web-customer/src/customer-account-integration.ts — View models and builders for all 7 backend modules:
    1. ProfileViewModel + buildProfileViewModel()
    2. OrdersViewModel + buildOrdersViewModel()
    3. BookingsViewModel + buildBookingsViewModel()
    4. AddressesViewModel + buildAddressesViewModel()
    5. PaymentMethodsViewModel + buildPaymentMethodsViewModel()
    6. LoyaltyViewModel + buildLoyaltyViewModel()
    7. NotificationsViewModel + buildNotificationsViewModel()
    8. AccountViewModel (aggregate) + createEmptyAccountViewModel()
  - platform/apps/web-customer/src/account-routes.ts — Added 3 new routes: /account/addresses, /account/payment-methods, /account/notifications
  - platform/apps/web-customer/src/account-shell.ts — Added 3 new empty state descriptors for addresses, payment-methods, notifications
  - platform/apps/web-customer/src/account-placeholders.ts — Added AddressesMountPoint, PaymentMethodsMountPoint, NotificationsMountPoint types; expanded accountSections from 4 to 7
  - platform/apps/web-customer/src/offline-route-behavior.ts — Added offline classifications for 3 new routes (all online-required)
- New or updated contracts: All 7 account tabs now have route definitions, empty states, mount point types, and view model builders.

## View Model States

Each view model supports 3 states:
- **idle**: No data loaded yet (initial state)
- **loaded**: Data available (may be empty — `isEmpty` flag for list views)
- **error**: Error occurred (errorMessage provided)

## All 7 Account Tabs

| Tab | Route | View Model | Empty State |
|-----|-------|-----------|-------------|
| Profile | /account/profile | ProfileViewModel | null profile |
| Orders | /account/orders | OrdersViewModel | "No Orders Yet" |
| Bookings | /account/bookings | BookingsViewModel | "No Bookings Yet" |
| Addresses | /account/addresses | AddressesViewModel | "No Saved Addresses" |
| Payment Methods | /account/payment-methods | PaymentMethodsViewModel | "No Payment Methods" |
| Loyalty | /account/loyalty | LoyaltyViewModel | tier progress bar |
| Notifications | /account/notifications | NotificationsViewModel | preference toggles |

## Payment Method Display Security

- PaymentMethodDisplayItem excludes gatewayToken — only shows cardBrand, lastFour, expiryLabel, isDefault
- formatPaymentMethodExpiry() formats as "MM/YYYY"

## Loyalty Progress Calculation

- Progress percent calculated as: (lifetimePoints - currentThreshold) / (nextThreshold - currentThreshold) × 100
- Capped at 100% for top tier (platinum)

## Validation

- Test layers run: pnpm --filter web-customer typecheck (clean), pnpm --filter web-customer test (545 tests pass)
- Key passing cases: All 7 view model builders with idle/loaded/error states, empty account creation, payment display formatting, loyalty progress calculation
- Playwright impact: Required — account pages are browser-visible. Smoke coverage extends existing web-customer-smoke project.

## Updated Existing Tests

- account-placeholders.test.ts: Updated from 4 → 7 sections
- account-shell.test.ts: Updated from 4 → 7 empty states, 8 → 11 children routes
- offline-route-behavior.test.ts: 3 new routes now classified

## Follow-Up Hooks

- Epic 9: Customer-facing onboarding may use the account shell
