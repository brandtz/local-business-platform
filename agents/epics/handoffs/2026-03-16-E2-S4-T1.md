# Handoff Note

## Header

- Task ID: E2-S4-T1
- Task Title: implement tenant resolution from domain, subdomain, or scoped admin context
- Status: completed
- Date: 2026-03-16
- Primary owner lane: Identity and Security Lane
- Supporting lane: Foundation Lane
- Packet path: ../packets/epic-02/E2-S4-T1.md

## What Changed

- Changed surfaces:
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../platform/packages/types/src/auth.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-host.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-host.test.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.test.ts
- New or updated contracts:
  - shared tenant-resolution types now distinguish `tenant`, `platform-admin`, and `unresolved` outcomes so later request-scope wiring can consume a trusted result instead of raw host input
  - tenant resolution now supports three trusted sources with explicit precedence: platform-admin domain detection, tenant-admin scoped tenant ID, then host-derived custom-domain or managed preview-subdomain lookup
  - platform-admin route space now fails closed on non-platform hosts instead of attempting tenant resolution
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/types test`
  - `pnpm --filter @platform/types typecheck`
  - `pnpm --filter @platform/types lint`
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
  - `pnpm --filter @platform/api lint`
- Key passing cases:
  - host normalization strips ports, lowercases, and handles forwarded-host style comma-separated values deterministically
  - tenant resolution prefers trusted tenant-admin context over conflicting host-derived matches
  - custom-domain, managed preview-subdomain, platform-admin host, and fail-closed platform-admin cases all pass under unit coverage
- Known untested cases and reason:
  - no live NestJS request object or controller guard consumes the resolver yet because request lifecycle injection belongs to E2-S4-T2
  - custom-domain persistence is still modeled as input data rather than a stored schema because domain management lands in Epic 3

## Assumptions and Risks

- Assumptions made:
  - scoped tenant-admin context is a more trusted signal than host headers and should therefore win before host-derived tenant matching
  - preview subdomain matching should accept only a single managed subdomain label and reject deeper chained hostnames until a more explicit routing model exists
- Risks introduced or still open:
  - resolver inputs are still in-memory records, so E2-S4-T2 must define how request scope loads the tenant directory safely and efficiently
  - tenant lifecycle-state enforcement for suspended or archived tenants remains open for E2-S4-T4 and Epic 3 lifecycle work
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E2-S4-T2
  - E2-S4-T3
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-request-host.ts
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../agents/architecture/system-overview.md
  - this handoff note
- Specific blocker or caution for next agent:
  - preserve the fail-closed behavior for platform-admin routes and do not allow downstream code to reconstruct tenant context from raw request params once a resolution result exists
- Board update required:
  - move E2-S4-T1 to Completed and clear the active claim before starting E2-S4-T2