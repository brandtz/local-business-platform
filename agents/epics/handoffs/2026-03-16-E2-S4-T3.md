# Handoff Note

## Header

- Task ID: E2-S4-T3
- Task Title: enforce tenant filters in repository and query helpers for tenant-scoped entities and real controller entry points
- Status: completed
- Date: 2026-03-16
- Primary owner lane: Identity and Security Lane
- Supporting lane: API Surface Lane
- Packet path: ../packets/epic-02/E2-S4-T3.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/request-context.service.ts
  - ../../../../platform/apps/api/src/auth/request-context.service.test.ts
  - ../../../../platform/apps/api/src/admin-tenant.controller.ts
  - ../../../../platform/apps/api/src/admin-tenant.controller.test.ts
  - ../../../../platform/apps/api/src/tenant-directory-query.service.ts
  - ../../../../platform/apps/api/src/tenant-directory-query.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - tenant request context now carries membership records alongside viewer identity and resolved tenant state
  - request context service now exposes a fail-closed `requireContext` helper for controller entry points
  - the API now includes an initial real tenant-admin controller for `GET /admin/tenant` and a tenant-scoped query helper that filters records by resolved tenant and requires membership before returning tenant data
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
  - `pnpm --filter @platform/api lint`
- Key passing cases:
  - `/admin/tenant` controller returns tenant data only when trusted request context is attached
  - missing request context fails closed at the controller boundary
  - tenant-scoped query helper filters cross-tenant records to the resolved tenant and still denies tenant reads when membership is absent
- Known untested cases and reason:
  - no live middleware populates request context during actual Nest HTTP execution yet because authenticated request pipeline wiring remains future work
  - unresolved, suspended, and archived tenant failure semantics are not differentiated yet because that belongs to E2-S4-T4

## Assumptions and Risks

- Assumptions made:
  - `GET /admin/tenant` is the narrowest useful real controller entry point to validate tenant-context enforcement without inventing broader admin CRUD early
  - membership records provided by the auth/session layer should be the authoritative source for tenant-admin access checks at controller-adjacent query services
- Risks introduced or still open:
  - downstream modules must keep using request context and should not regress into raw tenant-id inputs once richer repositories appear
  - suspended and archived tenant policy still needs explicit error behavior before this path is production-ready
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E2-S4-T4
  - E2-S5-T1
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/request-context.service.ts
  - ../../../../platform/apps/api/src/tenant-directory-query.service.ts
  - ../../../../platform/apps/api/src/admin-tenant.controller.ts
  - ../../../../platform/docs/access-control-model.md
  - this handoff note
- Specific blocker or caution for next agent:
  - failure-state handling for unresolved, suspended, and archived tenants should extend the request-context path rather than bypass it in controllers or query helpers
- Board update required:
  - move E2-S4-T3 to Completed and leave no active claim unless E2-S4-T4 is started immediately