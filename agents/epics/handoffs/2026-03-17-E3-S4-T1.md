# Handoff Note

## Header

- Task ID: E3-S4-T1
- Task Title: model custom domain records, verification state, and promotion state
- Status: completed
- Date: 2026-03-17
- Primary owner lane: Backend Domain Lane
- Supporting lane: Platform Control Lane
- Packet path: ../packets/epic-03/E3-S4-T1.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/prisma/schema.prisma
  - ../../../../platform/apps/api/src/app.module.ts
  - ../../../../platform/apps/api/src/auth/tenant-custom-domain-policy.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-custom-domain-policy.service.test.ts
  - ../../../../platform/apps/api/src/auth/tenant-provisioning.service.ts
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../platform/packages/types/src/auth.test.ts
- New or updated contracts:
  - Prisma now models tenant-bound `TenantCustomDomain` records with separate verification and promotion enums plus optional evidence and failure metadata.
  - Shared auth types now expose `TenantCustomDomainRecord`, `customDomainRecords`, custom-domain verification states, custom-domain promotion states, and transition helpers for both state machines.
  - API now exposes `TenantCustomDomainPolicyService` to validate allowed verification and promotion transitions without implementing DNS checks, primary-domain activation, or rollback execution.
- New config, env vars, events, or migrations:
  - no new env vars or events
  - schema contract updated in Prisma, but no generated migration was added in this task

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
  - `pnpm typecheck:contracts`
  - `pnpm --filter @platform/types test`
- Key passing cases:
  - API tests passed with new custom-domain transition coverage
  - custom domain verification states support pending, verified, failed, and denied outcomes
  - custom domain promotion states support not-requested, ready, promoted, failed, rollback-pending, rolled-back, and denied outcomes
  - invalid verification and promotion transitions are rejected without coupling this task to live DNS checks or tenant-routing activation behavior
- Known untested cases and reason:
  - no persistence-backed repository or migration execution path was exercised because this task only modeled the schema and contract surfaces
  - no DNS adapter, verification workflow, or primary-domain cutover flow was implemented because those are explicitly out of scope for E3-S4-T1

## Assumptions and Risks

- Assumptions made:
  - the existing `customDomains` hostname list remains in read models for current routing work, while new `customDomainRecords` carries lifecycle state for downstream verification and promotion tasks
  - tenant ownership of a hostname must not be inferred globally at this stage, so Prisma uniqueness is constrained to `[tenantId, hostname]` instead of a global hostname uniqueness rule
  - denial semantics are needed in both verification and promotion state sets now to avoid renaming churn in E3-S4-T2 and E3-S4-T3
- Risks introduced or still open:
  - runtime promotion eligibility is not enforced yet; future workflow code must still require a verified domain before moving into live-route activation behavior
  - current environment reports an engine warning because the workspace expects Node `>=22.12.0` but validation ran on Node `v22.11.0`
  - a real Prisma migration still needs to be generated when the database workflow for this repo is exercised
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E3-S4-T2
  - E3-S4-T3
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/prisma/schema.prisma
  - ../../../../platform/packages/types/src/auth.ts
  - ../../../../platform/apps/api/src/auth/tenant-custom-domain-policy.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - keep routing resolution on the existing hostname list until verification and promotion tasks intentionally switch consumers to lifecycle-aware domain records
  - do not treat the new schema model as proof of domain ownership across tenants; verification evidence must establish that explicitly in a later task
- Board update required:
  - no; handoff note only per overnight runbook and current task instructions