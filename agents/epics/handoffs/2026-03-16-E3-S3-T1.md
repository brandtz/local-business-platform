# Handoff Note

## Header

- Task ID: E3-S3-T1
- Task Title: define managed subdomain generation and uniqueness rules
- Status: completed
- Date: 2026-03-16
- Primary owner lane: API Surface Lane
- Supporting lane: Identity and Security Lane
- Packet path: ../packets/epic-03/E3-S3-T1.md

## What Changed

- Changed surfaces:
  - ../../../../platform/apps/api/src/auth/managed-subdomain.service.ts
  - ../../../../platform/apps/api/src/auth/managed-subdomain.service.test.ts
  - ../../../../platform/apps/api/src/app.module.ts
- New or updated contracts:
  - API now exposes a deterministic managed subdomain generation service with normalization, fallback, max-length, and collision suffix rules
- New config, env vars, events, or migrations:
  - none

## Validation

- Test layers run:
  - `pnpm --filter @platform/api test`
  - `pnpm --filter @platform/api typecheck`
  - `pnpm --filter @platform/api lint`
- Key passing cases:
  - preferred slugs normalize deterministically into valid subdomains
  - unicode and punctuation-heavy display names generate stable fallback subdomains
  - collisions append numeric suffixes deterministically
  - empty normalized candidates fall back to `tenant` and still handle collisions safely
- Known untested cases and reason:
  - subdomain generation is not yet wired into provisioning or preview routing resolution paths

## Assumptions and Risks

- Assumptions made:
  - a 32-character maximum and `-2`, `-3`, ... suffixing are acceptable current rules for managed preview subdomains
- Risks introduced or still open:
  - future routing and provisioning integration must ensure the same service is used everywhere to avoid divergent generated values
- Invariants changed: yes

## Next Handoff Targets

- Next likely task IDs:
  - E3-S3-T2
- Recommended first files or docs to read:
  - ../../../../platform/apps/api/src/auth/managed-subdomain.service.ts
  - ../../../../platform/apps/api/src/auth/tenant-resolution.service.ts
  - this handoff note
- Specific blocker or caution for next agent:
  - route resolution should consume generated subdomain rules without conflating preview and live-routing semantics
- Board update required:
  - move E3-S3-T1 to Completed and claim E3-S3-T2 if continuing immediately