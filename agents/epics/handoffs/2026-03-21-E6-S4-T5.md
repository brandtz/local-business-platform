# Handoff Note — E6-S4-T5

- Task ID: E6-S4-T5
- Task Title: Admin and Storefront Contracts
- Status: completed
- Date: 2026-03-21
- Primary owner lane: API Surface Lane
- Supporting lane: Backend Domain Lane
- Packet path: agents/epics/packets/epic-06/E6-S4-T3.md

## What Changed

- Changed surfaces: platform/apps/api/src/content/content-api-contracts.ts (new), platform/apps/api/src/content/content-api-contracts.test.ts (new)
- New or updated contracts:
  - ContentApiContractError — custom error for contract validation failures
  - assertValidCreateContentPageRequest — validates title (non-empty), slug (non-empty, valid format), body (required), optional SEO fields (seoTitle, seoDescription, ogImageUrl), optional templateRegion
  - assertValidUpdateContentPageRequest — validates partial updates, slug format when provided, optional fields
  - assertValidContentListQuery — validates pagination (page ≥ 1, pageSize 1-100), status must be valid ContentPageStatus, search must be string
  - assertValidStorefrontSlug — validates slug parameter for storefront by-slug endpoint
  - assertValidCreateAnnouncementRequest — validates title (non-empty), body (non-empty), placement must be valid type, displayPriority non-negative integer, date range ordering
  - assertValidUpdateAnnouncementRequest — validates partial updates, isActive boolean, placement type, date fields
- New config, env vars, events, or migrations: None

## Validation

- Test layers run: pnpm --filter @platform/api test (966 tests passing), pnpm typecheck:contracts (passing)
- Key passing cases: 
  - Create content page: accepts valid payload with body/title/slug, accepts optional SEO and template fields, accepts null optionals, rejects non-object/missing fields/invalid slug
  - Update content page: accepts partial updates including empty object, rejects invalid slug format and empty title
  - Content list query: accepts valid pagination and filters, rejects invalid page/pageSize/status
  - Storefront slug: accepts valid slugs, rejects empty/invalid/non-string
  - Create announcement: accepts valid payload, all placement types, optional date/priority, rejects end-before-start
  - Update announcement: accepts partial updates including isActive boolean, rejects invalid types
- Known untested cases and reason: HTTP-level endpoint integration (pure contract validation pattern, no route handler)

## Assumptions and Risks

- Assumptions made: Follows same assertion-based contract validation pattern as catalog-api-contracts.ts; storefront slug validation reused for public content retrieval; null is accepted for optional string fields (aligned with Prisma nullable pattern)
- Risks introduced or still open: None
- Invariants changed: no

## Next Handoff Targets

- Next likely task IDs: E6-S4-T6 (template rendering hooks), E6-S5 (vertical templates), E6-S6 (domain contract stabilization)
- Recommended first files or docs to read: platform/apps/api/src/content/content-api-contracts.ts, platform/apps/api/src/catalog/catalog-api-contracts.ts (established pattern)
- Specific blocker or caution for next agent: ContentListQuery type is local to the contracts file; SEO metadata shapes: seoTitle (string|null), seoDescription (string|null), ogImageUrl (string|null)
- Board update required: yes
