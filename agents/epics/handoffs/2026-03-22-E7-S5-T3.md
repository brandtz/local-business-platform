# Handoff Note — E7-S5-T3

- Task ID: E7-S5-T3
- Task Title: Saved Address CRUD
- Status: completed
- Date: 2026-03-22
- Primary owner lane: API Surface Lane

## What Changed

- Changed surfaces:
  - platform/packages/types/src/customer.ts — SavedAddress, CreateSavedAddressInput, UpdateSavedAddressInput types
  - platform/apps/api/src/customer/customer.service.ts — addAddress(), listAddresses(), updateAddress(), deleteAddress(), setDefaultAddress()
  - platform/apps/api/src/customer/customer.repository.ts — Address CRUD with default-clearing logic
  - platform/apps/api/src/customer/customer-api-contracts.ts — assertCreateSavedAddressInput, assertUpdateSavedAddressInput validators
- New or updated contracts: Saved address model with label, full US address fields (line1, line2, city, state, zip), isDefault flag. Address operations require valid customer profile in tenant.

## Address Model

- Fields: id, customerId, tenantId, label, line1, line2 (nullable), city, state, zip, isDefault, createdAt, updatedAt
- Set-default clears the previous default address before setting the new one
- Required fields for creation: label, line1, city, state, zip
- Partial update supported (any field optional)
- Tenant-scoped: addresses belong to a customer within a tenant

## Validation

- Test layers run: pnpm --filter @platform/api test (1486 tests pass)
- Key passing cases: Add, list, update, delete, set-default, default clearing, missing fields rejection, non-existent customer rejection
- Playwright impact: None (backend API)

## Follow-Up Hooks

- E7-S5-T7: Account shell renders saved addresses tab with empty/populated states
- Checkout delivery: address can be used as delivery address in order fulfillment
