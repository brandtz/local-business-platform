# E4-S6-T3 Task Packet

## Task

- Task ID: E4-S6-T3
- Title: align shared package usage so app-specific code remains thin and intentional
- Epic: 4 — Shared Frontend System and Customer Experience Shell
- Story: E4-S6 — Cross-App Frontend Conventions
- Owner lane: Frontend Systems Lane

## Scope

Ensure all three web apps (`web-customer`, `web-admin`, `web-platform-admin`) declare and wire the full set of shared packages:

- Add `@platform/ui`, `@platform/sdk`, and `@platform/utils` as workspace dependencies to each web app
- Update TypeScript project references in each app's tsconfig.json to reference `packages/types`, `packages/utils`, `packages/ui`, and `packages/sdk`
- Regenerate pnpm-lock.yaml to reflect the new dependency graph
- Confirm the existing workspace dependency-rules contract test still passes

## Acceptance Criteria

- All three web app package.json files list @platform/ui, @platform/sdk, @platform/utils, and @platform/types as dependencies
- All three web app tsconfig.json files reference the four shared package tsconfigs
- Existing dependency-rules contract test (`packages/config/src/dependency-rules.test.ts`) passes
- Existing package-manifest contract test passes
- pnpm install succeeds without errors
- No new runtime code in apps (dependency wiring only)

## Dependencies

- E4-S1-T1, E4-S1-T2 (new content in @platform/ui)
- E4-S6-T1 (new content in @platform/sdk)

## Output File

- `platform/apps/web-customer/package.json`
- `platform/apps/web-admin/package.json`
- `platform/apps/web-platform-admin/package.json`
- `platform/apps/web-customer/tsconfig.json`
- `platform/apps/web-admin/tsconfig.json`
- `platform/apps/web-platform-admin/tsconfig.json`
- `platform/pnpm-lock.yaml`
