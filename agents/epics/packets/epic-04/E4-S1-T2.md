# E4-S1-T2 Task Packet

## Task

- Task ID: E4-S1-T2
- Title: implement reusable layout primitives, navigation primitives, form primitives, and feedback components
- Epic: 4 — Shared Frontend System and Customer Experience Shell
- Story: E4-S1 — Design Tokens and Layout System
- Owner lane: Frontend Systems Lane

## Scope

Define shared component primitive types and factory functions in `@platform/ui` covering:

- Feedback primitives: `AlertDescriptor`, `EmptyStateDescriptor`, `StatusBannerDescriptor`
- Layout primitives: `PageLayoutDescriptor` (regions: header/sidebar/main/footer), `StackDescriptor`
- Shell state descriptors: `ShellStateDescriptor` covering loading/ready/error/empty/access-denied/auth-required/suspended
- Default messages for every shell state, with override support
- All factories follow the existing `createXyzDescriptor()` convention in `@platform/ui`

## Acceptance Criteria

- Types and factory functions exported from `@platform/ui`
- Unit tests verify variant enumeration, default values, and override behavior
- Shell state messages for every state are tested for completeness
- Copy safety: `getDefaultShellStateMessage()` returns a fresh copy
- No Vue or runtime framework dependency required

## Dependencies

- E4-S1-T1 (design tokens, for future CSS variable binding)

## Output File

- `platform/packages/ui/src/primitives.ts`
- `platform/packages/ui/src/primitives.test.ts`
