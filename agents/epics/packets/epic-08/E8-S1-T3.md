# Task Packet: E8-S1-T3

## Assignment

- Task ID: E8-S1-T3
- Epic: Epic 8
- Story: E8-S1 Payment Connection Management
- Objective: define tenant-admin APIs and forms for creating and managing gateway connections
- Primary owner lane: API Surface Lane
- Supporting lane: Frontend Systems Lane
- Validation lane: Verification Lane

## Required Inputs

- Dependencies already completed:
  - E8-S1-T1 payment connection schema
  - E8-S1-T2 connection services
- Required docs to read:
  - ../../epic-08-tasks.md
  - ../../handoffs/2026-03-17-E8-S1-T2.md
- Required code surfaces to inspect:
  - ../../../../platform/apps/api/src/admin-tenant.controller.ts
  - ../../../../platform/apps/web-admin/src/

## Expected Outputs

- Primary artifact: admin API endpoints for payment connections (list connections, create connection with credentials, verify connection, deactivate connection) and admin UI forms for connection setup — credentials input never echoed back in responses
- Secondary artifacts: request/response types (sanitized — never return secret values), admin form components, and tests
- Expected tests to add or update:
  - API contract tests: payloads validate, credentials are accepted but never returned
  - security tests: GET responses never include secret values, only connection status
  - form tests: connection setup form validates before submission
  - authorization tests: only admin roles can manage payment connections
- Validation commands:
  - pnpm --filter @platform/api test
  - pnpm --filter web-admin test
  - pnpm typecheck:contracts
- Playwright impact: yes — payment connection setup is admin-visible
- Required handoff note: use ../../handoff-note-template.md
- Handoff destination path: ../../handoffs/2026-03-17-E8-S1-T3.md

## Constraints

- Security-sensitive areas: critical
- Tenant-isolation considerations: connection management scoped via auth context
- Public contract considerations: admin API must never expose secrets in responses
- Areas explicitly out of scope:
  - health read models (E8-S1-T4)
  - payment processing UI
  - provider-specific OAuth flows

## Start Here

- First file or doc: ../../../../platform/apps/api/src/admin-tenant.controller.ts
- First validation check: admin API accepts credentials securely and never returns them in responses
- First downstream consumer to keep in mind: E8-S1-T4 health views and tenant admin users
